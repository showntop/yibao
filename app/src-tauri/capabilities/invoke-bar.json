{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "invoke-bar",
  "description": "唤起条窗最小权限：广播 invoke-action（emit）+ 失焦自隐（listen/hide；聚焦由 Rust 侧驱动）",
  "windows": [
    "invoke-bar"
  ],
  "permissions": [
    "core:event:default",
    "core:window:allow-hide"
  ]
}
