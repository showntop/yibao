{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "panel",
  "description": "面板窗最小权限：事件订阅（brain-event/聚焦）+ 标题栏拖动；自定义命令不受 ACL 约束无需列",
  "windows": [
    "panel"
  ],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-start-dragging"
  ]
}
