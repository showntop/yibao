# 面板可调方法白名单（v2 §7）：直调方法不过 LLM，过闸门
[[method]]
name = "delete"
handler = "notes.delete"
direct = true
refresh = "notes.list"   # 删除成功后跟一次查询，面板拿刷新后的列表

[[method]]
name = "list"
handler = "notes.list"
direct = true
