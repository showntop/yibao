# v1.1 Slice 2C（遗留功能补完 + 误报反馈回路）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补完 v1.1 承诺的三个遗留功能（watch_command 跨重启恢复、深色模式、截图唤起）+ 信任仪表写侧（误报反馈回路）。spec：`docs/superpowers/specs/2026-08-01-v1.1-consolidation-design.md` §6。

**Architecture:** 四个互相独立的功能流：A) BackgroundJobManager 落 SQLite（FeedStore 同款模式）+ 启动恢复钩子；B) tokens.css dark 变体（prefers-color-scheme）+ 5 个文件的硬编码颜色清令牌化；C) 唤起时截图→vision 一句话描述→暂存→下次 run 注入上下文（无视觉配置静默跳过）；D) Feed 条目 👍/👎 → meta.feedback → 同类 24h≥2 👎 的主动事件降级 quiet。

**Tech Stack:** Python（sidecar）+ pytest、Rust（Tauri）、Vue3+TS。

## Global Constraints

- sidecar 测试：`cd sidecar && uv run --extra dev pytest -q`（必须带 `--extra dev`）；前端验证 `cd app && npm run build`；Rust 验证 `cd app/src-tauri && cargo check`。
- 提交信息：中文 conventional commit，每任务一提交。
- Feed/jobs 存储遵循 FeedStore 模式：`check_same_thread=False` + threading.Lock + 幂等迁移 + 写失败只 print 不抛。
- 主动行为防打扰原则不变：所有新主动行为只记账不新增打扰面；L3 命令的自动重跑只在「大脑重启恢复」场景，且必须 Feed 记账。
- 深色模式走系统跟随（`prefers-color-scheme`），不加设置开关（YAGNI）。

---

### Task 1: watch_command 跨重启恢复

**Files:**
- Create: `sidecar/src/yibao_brain/jobstore.py`（JobsStore：jobs 表持久化）
- Modify: `sidecar/src/yibao_brain/background_jobs.py`（store 钩子：start 落库、完成更新、recover_orphans）
- Modify: `sidecar/src/yibao_brain/server.py:594` 后（启动恢复钩子）
- Test: `sidecar/tests/test_jobstore.py`（新）、`sidecar/tests/test_background_jobs.py`

**Interfaces:**
- Produces:
  - `JobsStore(db_path)`：表 `jobs(task_id TEXT PK, command, cwd, name, timeout REAL, status, exit_code INT, output_tail, started_at REAL, finished_at REAL)`；方法 `add(job: dict)` / `finish(task_id, *, status, exit_code, output_tail)` / `running() -> list[dict]` / `mark_interrupted(task_id)`；写失败只 print。
  - `BackgroundJobManager(*, tail_chars=80, store: JobsStore | None = None)`：start 时 `store.add(job)`（线程启动前）；`_run` 收尾 `store.finish(...)`；新增 `recover_orphans(restart: Callable[[dict], dict | None]) -> list[dict]`：把 store.running() 逐条 `mark_interrupted` 后交给 restart 回调，返回处理结果。
  - server 启动钩子：`_recover_background_jobs()`——orphan 逐条：能重跑（cwd 存在）→ `jobs.start(...)` 同参数 + Feed 记账「大脑重启，已重新执行」；否则 `mark_interrupted` + Feed「因重启中断，未重跑（原因）」。

- [ ] **Step 1: 写失败测试**

`sidecar/tests/test_jobstore.py`：

```python
"""JobsStore：watch_command 任务持久化（FeedStore 模式）。"""


def test_add_finish_running_roundtrip(tmp_path):
    from yibao_brain.jobstore import JobsStore

    store = JobsStore(str(tmp_path / "jobs.db"))
    store.add({"task_id": "job_a", "command": "sleep 5", "cwd": "/tmp", "name": "测试",
               "timeout": 600.0, "status": "running", "exit_code": None,
               "output_tail": "", "started_at": 100.0, "finished_at": None})
    assert [j["task_id"] for j in store.running()] == ["job_a"]
    store.finish("job_a", status="completed", exit_code=0, output_tail="done")
    assert store.running() == []
    rows = [j for j in store.running()]
    store.close()


def test_mark_interrupted(tmp_path):
    from yibao_brain.jobstore import JobsStore

    store = JobsStore(str(tmp_path / "jobs.db"))
    store.add({"task_id": "job_b", "command": "x", "cwd": "/tmp", "name": "n",
               "timeout": 1.0, "status": "running", "exit_code": None,
               "output_tail": "", "started_at": 1.0, "finished_at": None})
    store.mark_interrupted("job_b")
    assert store.running() == []
    store.close()


def test_store_failure_degrades_silently(tmp_path):
    """DB 路径不可写 → 只 print 不抛（Feed 增强面原则）。"""
    from yibao_brain.jobstore import JobsStore

    store = JobsStore("/nope/no/such/dir/jobs.db")  # 构造时建目录失败也不抛
    store.add({"task_id": "j", "command": "x", "cwd": "/tmp", "name": "n",
               "timeout": 1.0, "status": "running", "exit_code": None,
               "output_tail": "", "started_at": 1.0, "finished_at": None})
    assert store.running() == []
    store.close()
```

`sidecar/tests/test_background_jobs.py` 追加：

```python
def test_manager_persists_start_and_finish(tmp_path):
    """start 落库 running；完成更新终态。"""
    from yibao_brain.background_jobs import BackgroundJobManager
    from yibao_brain.jobstore import JobsStore

    store = JobsStore(str(tmp_path / "jobs.db"))
    manager = BackgroundJobManager(store=store)
    job = manager.start("exit 0", cwd=str(tmp_path), name="p")
    assert [j["task_id"] for j in store.running()] == [job["task_id"]]
    _wait_for(manager, job["task_id"], {"completed"})
    assert store.running() == []
    manager.shutdown()
    store.close()


def test_recover_orphans_restarts_and_marks(tmp_path):
    """孤儿任务：可重跑的重跑（Feed 记账），cwd 消失的标 interrupted。"""
    from yibao_brain.background_jobs import BackgroundJobManager
    from yibao_brain.jobstore import JobsStore

    store = JobsStore(str(tmp_path / "jobs.db"))
    m1 = BackgroundJobManager(store=store)
    j1 = m1.start("sleep 30", cwd=str(tmp_path), name="可重跑")
    j2 = m1.start("sleep 30", cwd="/definitely/not/exist", name="不可重跑") \
        if False else None  # start 会拒绝坏 cwd——直接手工落库模拟孤儿
    store.add({"task_id": "job_gone", "command": "sleep 30", "cwd": "/definitely/not/exist",
               "name": "不可重跑", "timeout": 600.0, "status": "running", "exit_code": None,
               "output_tail": "", "started_at": 1.0, "finished_at": None})
    # 不 shutdown m1（模拟崩溃）；新 manager 接管同库
    m2 = BackgroundJobManager(store=store)
    results = m2.recover_orphans(
        restart=lambda orphan: m2.start(orphan["command"], cwd=orphan["cwd"],
                                        name=orphan["name"], timeout=orphan["timeout"]))
    by_id = {r["orphan"]: r["outcome"] for r in results}
    assert by_id[j1["task_id"]] == "restarted"
    assert by_id["job_gone"] == "interrupted"
    m2.shutdown()
    store.close()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && uv run --extra dev pytest tests/test_jobstore.py tests/test_background_jobs.py -q`
Expected: FAIL（jobstore 不存在 / manager 无 store 参数）

- [ ] **Step 3: 实现 jobstore.py**

```python
"""JobsStore：watch_command 后台任务持久化（跨重启恢复，v1.1）。

FeedStore 同款模式：check_same_thread=False + 锁 + 幂等迁移 + 写失败只 print 不抛——
任务恢复是增强面，永远不许拖垮启动链路。
"""
from __future__ import annotations

import os
import sqlite3
import sys
import threading
import time

_SCHEMA = """
CREATE TABLE IF NOT EXISTS jobs (
  task_id TEXT PRIMARY KEY,
  command TEXT NOT NULL,
  cwd TEXT NOT NULL,
  name TEXT NOT NULL,
  timeout REAL NOT NULL,
  status TEXT NOT NULL,
  exit_code INTEGER,
  output_tail TEXT NOT NULL DEFAULT '',
  started_at REAL NOT NULL,
  finished_at REAL
);
"""


class JobsStore:
    def __init__(self, db_path: str):
        self._conn = None
        self._lock = threading.Lock()
        try:
            os.makedirs(os.path.dirname(db_path), exist_ok=True)
            self._conn = sqlite3.connect(db_path, check_same_thread=False)
            self._conn.row_factory = sqlite3.Row
            with self._lock:
                self._conn.executescript(_SCHEMA)
        except Exception as e:
            print(f"[yibao] jobs 存储初始化失败（已降级为不持久化）：{e}", file=sys.stderr)

    def add(self, job: dict) -> None:
        if self._conn is None:
            return
        try:
            with self._lock:
                self._conn.execute(
                    "INSERT OR REPLACE INTO jobs (task_id, command, cwd, name, timeout, status,"
                    " exit_code, output_tail, started_at, finished_at) VALUES (?,?,?,?,?,?,?,?,?,?)",
                    (job["task_id"], job["command"], job["cwd"], job["name"], job["timeout"],
                     job["status"], job["exit_code"], job["output_tail"], job["started_at"],
                     job["finished_at"]),
                )
                self._conn.commit()
        except Exception as e:
            print(f"[yibao] jobs 写入失败（已跳过）：{e}", file=sys.stderr)

    def finish(self, task_id: str, *, status: str, exit_code, output_tail: str) -> None:
        if self._conn is None:
            return
        try:
            with self._lock:
                self._conn.execute(
                    "UPDATE jobs SET status=?, exit_code=?, output_tail=?, finished_at=? WHERE task_id=?",
                    (status, exit_code, output_tail, time.time(), task_id),
                )
                self._conn.commit()
        except Exception as e:
            print(f"[yibao] jobs 更新失败（已跳过）：{e}", file=sys.stderr)

    def running(self) -> list[dict]:
        if self._conn is None:
            return []
        try:
            with self._lock:
                rows = self._conn.execute(
                    "SELECT * FROM jobs WHERE status = 'running' ORDER BY started_at"
                ).fetchall()
            return [dict(r) for r in rows]
        except Exception as e:
            print(f"[yibao] jobs 查询失败（已降级为空）：{e}", file=sys.stderr)
            return []

    def mark_interrupted(self, task_id: str) -> None:
        if self._conn is None:
            return
        try:
            with self._lock:
                self._conn.execute(
                    "UPDATE jobs SET status='interrupted', finished_at=? WHERE task_id=?",
                    (time.time(), task_id),
                )
                self._conn.commit()
        except Exception as e:
            print(f"[yibao] jobs 标记失败（已跳过）：{e}", file=sys.stderr)

    def close(self) -> None:
        if self._conn is not None:
            with self._lock:
                self._conn.close()
```

- [ ] **Step 4: 实现 background_jobs.py 钩子**

`BackgroundJobManager.__init__` 加 `store=None` 参数（`self._store = store`）；`start()` 在 `thread.start()` 前加 `if self._store: self._store.add(self._public(job))`；`_run` 收尾 `job.update(...)` 之后加 `if self._store: self._store.finish(task_id, status=status, exit_code=exit_code, output_tail=tail)`；新增：

```python
    def recover_orphans(self, restart) -> list[dict]:
        """把 store 里残留的 running 任务（上代进程遗物）逐条处置：
        先标 interrupted，再交 restart 回调尝试重跑（回调内部失败 → 保持 interrupted）。
        返回 [{"orphan": task_id, "outcome": "restarted"|"interrupted"}]。"""
        if self._store is None:
            return []
        results = []
        for orphan in self._store.running():
            task_id = orphan["task_id"]
            self._store.mark_interrupted(task_id)
            outcome = "interrupted"
            try:
                restarted = restart(orphan)
                if restarted is not None:
                    outcome = "restarted"
            except Exception:
                pass
            results.append({"orphan": task_id, "outcome": outcome})
        return results
```

- [ ] **Step 5: server 启动钩子**

`server.py:594`（`agent.invoker.emit_event = _emit_event`）之后插入：

```python
    # watch_command 跨重启恢复：孤儿任务重跑或标失败，全部 Feed 记账
    jobs_store = JobsStore(os.path.join(os.path.dirname(db_path), "jobs.db"))
    if getattr(agent.invoker, "background_jobs", None) is not None:
        agent.invoker.background_jobs._store = jobs_store
        def _restart(orphan):
            if not os.path.isdir(orphan["cwd"]):
                return None
            return agent.invoker.background_jobs.start(
                orphan["command"], cwd=orphan["cwd"], name=orphan["name"],
                timeout=orphan["timeout"], emit=_emit_event)
        for r in agent.invoker.background_jobs.recover_orphans(restart=_restart):
            if r["outcome"] == "restarted":
                feed.add("event", f"大脑重启，后台任务已重新执行（原 {r['orphan']}）", {"type": "watch_command"})
            else:
                feed.add("event", f"后台任务因大脑重启中断，未重跑（{r['orphan']}）", {"type": "watch_command"})
```

（import JobsStore 于 server.py 顶部；`background_jobs` 在 invoker 上的挂载点若无，用 `register_real_skills` 返回的 jobs 变量——按实际持有路径接线。）

- [ ] **Step 6: 跑测试确认通过 + 全量 + 重启三次验证**

Run: `cd sidecar && uv run --extra dev pytest -q`
Expected: PASS。再手动跑一次 `uv run --extra dev pytest tests/test_background_jobs.py::test_recover_orphans_restarts_and_marks --count 3`（无 count 插件则手动三次）确认确定性。

- [ ] **Step 7: Commit**

```bash
git add sidecar/src/yibao_brain/jobstore.py sidecar/src/yibao_brain/background_jobs.py sidecar/src/yibao_brain/server.py sidecar/tests/
git commit -m "feat(watch): watch_command 跨重启恢复——任务落 SQLite + 孤儿重跑/标失败"
```

---

### Task 2: 深色模式（tokens dark 变体 + 硬编码清零）

**Files:**
- Modify: `app/src/assets/tokens.css`（启用并补全 dark 变体，:262 color-scheme）
- Modify: `app/src/components/Avatar.vue`、`app/src/App.vue`、`app/src/components/SchemaPanel.vue`、`app/src/components/InputBar.vue`、`app/src/components/SettingsView.vue`（硬编码颜色→语义令牌）

**Interfaces:**
- Produces: `@media (prefers-color-scheme: dark)` 下完整的语义令牌覆盖（表面/文字/描边/主色/intent/阴影/气泡/代码/形象身体/state 十组，值参照 tokens.css:302-320 注释草案补齐）；`color-scheme: light dark`。五文件内联 hex/rgb 颜色全部改 `var(--yb-*)`。

- [ ] **Step 1: tokens.css dark 变体**

把 :302-320 的注释草案转为 `@media (prefers-color-scheme: dark) { :root { ... } }` 形式，对照 :106-201 的令牌清单逐组补齐缺项（草案没有的组沿用浅色值也可，但表面/文字/描边/主色必须有）；`:root { color-scheme: light }` 改 `color-scheme: light dark`。
验证：无自动化——`cd app && npm run build` 通过即可。

- [ ] **Step 2: 五文件硬编码颜色审计清零**

逐文件把 `#xxx`/`rgb(...)` 内联颜色替换为语义令牌（表面→--yb-surface-*、文字→--yb-text-*、描边→--yb-border-*、主色→--yb-accent*）。实在无语义对应的（如形象身体的渐变色）用 dark 变体里补令牌而不是保留硬编码。完成后验证：

```bash
grep -rEn "#[0-9a-fA-F]{3,8}\b|rgb\(" app/src --include="*.vue" | grep -v "var(--yb" | wc -l
```
Expected: ≤ 少量（剩余须逐一说明为何保留，如 SVG 内固定色）。

- [ ] **Step 3: 构建 + 目验**

`cd app && npm run build` 通过；`npm run tauri dev` 把 macOS 外观切深色，目验三页（Home/插件/设置）+ 桌宠无刺眼色块。目验结果记录在报告。

- [ ] **Step 4: Commit**

```bash
git add app/src/assets/tokens.css app/src/components/ app/src/App.vue
git commit -m "feat(ui): 深色模式——tokens dark 变体系统跟随 + 硬编码颜色清令牌化"
```

---

### Task 3: 截图唤起（唤起抓屏 → vision 描述 → 下次 run 注入）

**Files:**
- Modify: `app/src-tauri/src/lib.rs`（pet-show 时前端可触发 invoke_context 命令）
- Modify: `app/src/lib/brain.ts`（invokeContext()）
- Modify: `app/src/App.vue:603-604`（pet-show 监听里调 invokeContext）
- Modify: `sidecar/src/yibao_brain/server.py`（`invoke_context` 分支 + run 分支注入）
- Test: `sidecar/tests/test_server.py`

**Interfaces:**
- Produces: 壳→脑 `{"type":"invoke_context"}`；sidecar：抓屏（host.screenshotter.capture → b64）→ vision 一句话描述（复用 _vision_create_with_retry，prompt 自定义）→ 暂存 `{"text","ts"}`；run 分支暂存新鲜（<60s）则把描述以 `[屏幕上下文] ...` 前缀拼进 run 文本并清空暂存；无截图能力/无视觉配置/描述失败一律静默跳过（不影响 run）。**前端仅热键唤起主窗时触发**（⌘⇧U 划词路径不触发）。

- [ ] **Step 1: 写失败测试（sidecar）**

`sidecar/tests/test_server.py` 追加：

```python
def test_serve_async_invoke_context_injects_into_next_run(tmp_path):
    """invoke_context 暂存描述 → 下一次 run 的文本带 [屏幕上下文] 前缀且暂存清空。"""
    out = []
    _run_async(
        serve_async(
            make_reader([
                {"type": "invoke_context"},
                {"type": "run", "text": "这个报错什么意思"},
            ]),
            lambda m: out.append(m),
            use_real=False,
            db_path=str(tmp_path / "a.db"),
            provider=FakeProvider(),
            invoke_context_text="用户在看 VS Code 的报错弹窗",  # 测试注入点（见实现）
        )
    )
    runs = [m for m in out if m.get("type") == "run_done"]
    assert runs  # run 完成
    # 暂存清空：第二个 run 不再带前缀（由实现暴露的查询或二次 run 验证）
```

（注入点设计：serve_async 加仅测试用参数 `invoke_context_text`——生产 None 时走真实截图+vision；无截图/视觉配置静默跳过。这样单测不依赖真机。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && uv run --extra dev pytest tests/test_server.py -q -k invoke_context`
Expected: FAIL（未知消息类型被忽略）

- [ ] **Step 3: sidecar 实现**

`server.py`：
1. serve_async 签名加 `invoke_context_text: str | None = None`；函数前部 `invoke_ctx = {"text": invoke_context_text, "ts": time.time() if invoke_context_text else 0}`。
2. 分发链加分支：

```python
        elif rtype == "invoke_context":
            # 唤起抓屏描述（无视觉配置/截图失败静默跳过）；测试可注入 invoke_context_text
            if invoke_context_text is None and host is not None and vision is not None:
                try:
                    shot = host.screenshotter.capture()
                    with open(shot, "rb") as f:
                        import base64 as _b64mod
                        b64 = "data:image/png;base64," + _b64mod.b64encode(f.read()).decode()
                    desc = _describe_screen(vision, b64)  # 一句话描述；失败返 None
                    if desc:
                        invoke_ctx.update({"text": desc, "ts": time.time()})
                except Exception as e:
                    print(f"[yibao] 唤起抓屏失败（已跳过）：{e}", file=sys.stderr)
```

3. run 分支（:1048 附近）注入：

```python
            if invoke_ctx["text"] and time.time() - invoke_ctx["ts"] < 60:
                text = f"[屏幕上下文] {invoke_ctx['text']}\n\n{text}"
            invoke_ctx.update({"text": None, "ts": 0})
```

4. `_describe_screen(client, b64) -> str | None`：复用 `_vision_create_with_retry`，system prompt「用一句话（20 字内）描述用户屏幕上正在发生什么，只输出这句话」；异常返 None。

- [ ] **Step 4: Rust + 前端接线**

lib.rs：`pet-show` 不做新动作——前端 App.vue `listen("pet-show")` 已有；brain.ts 加 `export function invokeContext() { return invoke("invoke_context"); }`；lib.rs 加命令：

```rust
/// 唤起上下文：通知大脑抓屏描述（下次 run 注入屏幕上下文）。
#[tauri::command]
fn invoke_context(state: tauri::State<Brain>) -> Result<(), String> {
    write_to_brain(&state, serde_json::json!({ "id": 0, "type": "invoke_context" }))
}
```

注册进 invoke_handler；App.vue onPetShow 里 `void invokeContext();`（仅 ⌘⇧Y 主窗唤起路径）。

- [ ] **Step 5: 验证**

`cd sidecar && uv run --extra dev pytest -q` 全绿；`cd app/src-tauri && cargo check`；`cd app && npm run build`。

- [ ] **Step 6: Commit**

```bash
git add sidecar/src/yibao_brain/server.py sidecar/tests/test_server.py app/src-tauri/src/lib.rs app/src/lib/brain.ts app/src/App.vue
git commit -m "feat(invoke): 截图唤起——抓屏 vision 描述暂存，下次 run 注入屏幕上下文"
```

---

### Task 4: 误报反馈回路（Feed 👍/👎 → 同类降频）

**Files:**
- Modify: `sidecar/src/yibao_brain/feed.py`（set_feedback/count_feedback_by_type）
- Modify: `sidecar/src/yibao_brain/proactive.py`（dispatch 降频闸门 + meta.type 写入）
- Modify: `sidecar/src/yibao_brain/watch.py`（事件加 type：health_nudge/proactive_chat）、`background_jobs.py`（type: watch_command）
- Modify: `sidecar/src/yibao_brain/server.py`（feed_feedback 分支）
- Modify: `app/src-tauri/src/lib.rs`（feed_feedback 命令 + 转发）、`app/src/lib/brain.ts`、`app/src/components/HomeFeed.vue`（tl-acts 加 👍/👎）
- Test: `sidecar/tests/test_feed.py`、`sidecar/tests/test_proactive.py`、`sidecar/tests/test_server.py`

**Interfaces:**
- Produces:
  - `FeedStore.set_feedback(feed_id, feedback: str) -> bool`（"up"/"down"/"none"，写 meta.feedback=json_set）；`count_feedback_by_type(mtype, feedback, since) -> int`
  - 事件 type 归一：watch 事件带 `type`（health_nudge/proactive_chat）、后台命令带 `watch_command`、提醒插件事件带 `reminder`；dispatcher 写 Feed 时 meta 并入 `{"type": event.get("type")}`（无则不带）
  - 降频规则（确定性）：reminder 类事件 dispatch 时，若同类（meta.type）24h 内 feedback=down ≥2 → 本次降级为 quiet（Feed 仍记账，不弹不播）
  - IPC：壳→脑 `{"type":"feed_feedback","id":N,"feedback":"up|down|none"}` → 回 `{"type":"feed_feedback_set","id","ok"}` → Rust 转发 `brain-feed-feedback-set`

- [ ] **Step 1: 写失败测试（feed + proactive）**

`test_feed.py` 追加：

```python
def test_set_feedback_and_count_by_type(tmp_path):
    from yibao_brain.feed import FeedStore

    feed = FeedStore(str(tmp_path / "f.db"))
    feed.add("reminder", "坐久了", {"type": "health_nudge"})
    feed.add("reminder", "又坐久了", {"type": "health_nudge"})
    feed.add("event", "任务完成", {"type": "watch_command"})
    assert feed.set_feedback(1, "down") is True
    assert feed.set_feedback(2, "down") is True
    assert feed.set_feedback(3, "up") is True
    assert feed.set_feedback(999, "down") is False
    assert feed.set_feedback(1, "bad") is False
    now = time.time()
    assert feed.count_feedback_by_type("health_nudge", "down", now - 86400) == 2
    assert feed.count_feedback_by_type("watch_command", "down", now - 86400) == 0
    feed.close()
```

`test_proactive.py` 追加：

```python
def test_dispatcher_downgrades_when_type_got_two_downvotes():
    """同类 24h≥2 👎 → 本次 reminder 降级 quiet（Feed 仍记账，write_msg 不出）。"""
    async def run():
        feed, messages = _Feed(), []
        feed.items.append(("reminder", "x", {"type": "health_nudge", "feedback": "down"}))
        feed.items.append(("reminder", "y", {"type": "health_nudge", "feedback": "down"}))
        dispatcher = ProactiveDispatcher(
            settings={"proactive.level": "full"}, feed=feed, write_msg=messages.append,
        )
        await dispatcher.dispatch({"kind": "reminder", "text": "break", "type": "health_nudge"})
        assert messages == []          # 降级 quiet：不弹
        assert feed.items[-1][1] == "break"  # 但仍记账
    asyncio.run(run())
```

（_Feed 需要相应方法支撑计数查询——按实现对齐 fake；真实规则走 count_feedback_by_type。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && uv run --extra dev pytest tests/test_feed.py tests/test_proactive.py -q -k feedback or downgrad`
Expected: FAIL

- [ ] **Step 3: sidecar 实现**

1. `feed.py`：

```python
    def set_feedback(self, feed_id: int, feedback: str) -> bool:
        """写 meta.feedback（up/down/none）。写失败只 print 不抛，返回 False。"""
        if feedback not in ("up", "down", "none"):
            return False
        try:
            with self._lock:
                cur = self._conn.execute(
                    "UPDATE feed SET meta = json_set(meta, '$.feedback', ?) WHERE id = ?",
                    (feedback, feed_id),
                )
                self._conn.commit()
            return cur.rowcount > 0
        except Exception as e:
            print(f"[yibao] feed 反馈写入失败（已跳过）：{e}", file=sys.stderr)
            return False

    def count_feedback_by_type(self, mtype: str, feedback: str, since: float) -> int:
        """近 since 以来同类条目的某反馈数（降频判断用）。读失败降级 0。"""
        try:
            with self._lock:
                row = self._conn.execute(
                    "SELECT COUNT(*) AS n FROM feed WHERE json_extract(meta,'$.type') = ? "
                    "AND json_extract(meta,'$.feedback') = ? AND ts >= ?",
                    (mtype, feedback, since),
                ).fetchone()
            return int(row["n"]) if row else 0
        except Exception as e:
            print(f"[yibao] feed 反馈计数失败（已降级为 0）：{e}", file=sys.stderr)
            return 0
```

2. `watch.py`：HealthNudge 事件加 `"type": "health_nudge"`；`_proactive_look` 的 emit 加 `"type": "proactive_chat"`。`background_jobs.py` 完成 emit 加 `"type": "watch_command"`。
3. `proactive.py` dispatch：feed.add 的 meta 改 `{**task_meta, **({"type": event["type"]} if event.get("type") else {})}`；在 `if event.get("kind") != "reminder":` 之后、level 判断之前加：

```python
        etype = event.get("type")
        if etype and self._downvotes(etype) >= 2:
            return  # 同类 24h≥2 👎 → 降级 quiet（Feed 已记账，不弹不播）
```

`_downvotes(self, etype) -> int`：feed 有 count_feedback_by_type 才查（fake 兼容），`time.time() - 86400`。

4. `server.py` 分发链加：

```python
        elif rtype == "feed_feedback":
            fid = int(msg.get("id", 0))
            ok = feed.set_feedback(fid, str(msg.get("feedback", "none")))
            write_msg({"type": "feed_feedback_set", "id": fid, "ok": ok})
```

- [ ] **Step 4: Rust + 前端**

lib.rs：`Some("feed_feedback_set") => emit("brain-feed-feedback-set", v)` 转发 + `feed_feedback` 命令（照 feed_mark_status 模式）+ 注册。brain.ts：`sendFeedFeedback(id, feedback)` invoke。HomeFeed.vue tl-acts 在跟进/忽略旁加 👍/👎 按钮：点击 `sendFeedFeedback(it.id, 'up'|'down')` + 乐观在条目上标记（样式复用 tl-acts 现有类）。

- [ ] **Step 5: 验证 + Commit**

`cd sidecar && uv run --extra dev pytest -q`；`cd app/src-tauri && cargo check`；`cd app && npm run build`。

```bash
git add sidecar/src/yibao_brain/ sidecar/tests/ app/src-tauri/src/lib.rs app/src/lib/brain.ts app/src/components/HomeFeed.vue
git commit -m "feat(feed): 误报反馈回路——条目 👍/👎 落 meta，同类 24h≥2👎 主动事件降 quiet"
```

---

## Self-Review 记录

- **Spec 覆盖**：spec §6 的 watch 恢复 → Task 1；深色模式 → Task 2；截图唤起 → Task 3；误报反馈 → Task 4。达标线「重启 3 次 100% 恢复或明确标记」→ Task 1 Step 6。
- **占位符扫描**：Task 3 Step 1 测试的注入点（invoke_context_text）与 Task 4 Step 1 的 _Feed 对齐说明是有意的接口约定而非占位；其余代码完整。
- **类型一致性**：事件 type 字符串四处一致（watch.py/background_jobs.py/proactive.py/前端）；feed_feedback IPC 形状三端一致；JobsStore 字段与 BackgroundJobManager._public 键一致。
- **范围**：四功能独立可分别评审；无 spec 外夹带（不加深色开关、不改 run 协议结构）。
