# Coding 线产品化（第二轮：缺口扫平）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** rewind 检查点回滚、plan mode 切换、can_use_tool 权限交互、@files 上下文、transcript 落库——五个缺口一次做完，统一验收。

**Architecture:** 全部走 SDK 0.2.129 官方通道（enable_file_checkpointing + replay-user-messages + rewind_files / set_permission_mode / can_use_tool / interrupt）+ sessions 旁新增 messages 表（transcript + checkpoint 原料）+ 面板（mode pill、用户气泡 ⏪、审批卡、@ 补全）。

**Tech Stack:** Python（claude-agent-sdk 0.2.129/pytest）、原生 JS（chat.html iframe）。

**关联 spec：** `docs/superpowers/specs/2026-08-09-coding-r2-design.md`

## Global Constraints

- **SDK 交互 duck-typing 防御**：uuid/checkpoint/usage 拿不到就降级，不阻断流、不炸 runner。
- **权限回调异常一律 deny**（安全默认）+ stderr；裁决超时 60s 默认 deny。
- **历史读回两级降级**：库 → _cc_reader → 空，不许把「恢复」变「报错」。
- **sidecar pytest 全绿（基线 874→878）；vite build exit 0；不改 Rust**。
- **commit**：每任务一 commit，中文 scope（`feat(coding)`/`fix(coding)`），仅 stage 本任务文件，不动 `.gitignore`，提交在 `feat/coding-r2` 分支。
- FakeSDK/注入模式测试，不触真 SDK、不触真 ~/.claude。

## File Structure

**改：**
- `plugins/coding/skills/_runner.py`（checkpointing/replay/interrupt/mode/can_use_tool/user_msg uuid/rewind_pending）
- `plugins/coding/skills/coding.py`（mode 落库透传、ModeSkill/RewindSkill/DecideSkill/FilesSkill、_SESSIONS 扩展）
- `plugins/coding/manifest.toml`（sessions+mode 列、messages 表）
- `plugins/coding/api.toml`（mode/rewind/decide/files quiet 条目）
- `plugins/coding/panel/chat.html`（mode pill、⏪ 按钮、审批卡、@ 补全）
- `sidecar/tests/test_coding_plugin.py`、`sidecar/tests/test_coding_handoff.py`

---

### Task 1: 接线升级（checkpointing + replay + interrupt + mode 参数 + user_msg uuid）

**Files:**
- Modify: `plugins/coding/skills/_runner.py`（_default_factory :115-120、run :122-155、normalize User 分支）
- Test: `sidecar/tests/test_coding_plugin.py`（追加）

**Interfaces:**
- Consumes: SDK 0.2.129（`enable_file_checkpointing`、`extra_args={"replay-user-messages": None}`、`client.interrupt()`、`permission_mode`）
- Produces:
  - normalize UserMessage → `{"kind":"user_msg","uuid":str,"text":str}`（无 uuid 时无此键，tool_result 提取不受影响）
  - runner `run(..., permission_mode: str = "acceptEdits", can_use_tool=None)` 参数
  - 取消路径：先发 stopped 前 `await client.interrupt()`（鸭子类型 `getattr(client,"interrupt",None)`，失败静默）
  - runner run 返回类型不变（str | None = cc_session_id）

- [ ] **Step 1: 写失败测试**

`sidecar/tests/test_coding_plugin.py` 追加（复用既有 FakeSDK/工厂模式）：

```python
def test_factory_passes_checkpointing_and_replay_args():
    """生产 options：checkpointing 开 + replay-user-messages（rewind 原料）；mode 可参数化。"""
    seen = {}
    def factory(cwd, tools, resume=None, permission_mode="acceptEdits", can_use_tool=None):
        seen["mode"] = permission_mode
        opts = ClaudeAgentOptions(
            cwd=cwd, permission_mode=permission_mode, allowed_tools=tools, resume=resume,
            enable_file_checkpointing=True, extra_args={"replay-user-messages": None},
            can_use_tool=can_use_tool,
        )
        seen["opts"] = opts
        return _FakeClient(opts)
    runner = ClaudeCodeRunner(client_factory=factory)
    _run(runner.run("p", "/tmp", on_event=lambda e: None, cancel_event=_NeverSet(), permission_mode="plan"))
    assert seen["mode"] == "plan"
    assert seen["opts"].enable_file_checkpointing is True
    assert seen["opts"].extra_args == {"replay-user-messages": None}


def test_user_message_yields_user_msg_event_with_uuid():
    """replay 开启后 UserMessage（无 tool_result 块）→ user_msg 事件带 uuid；无 uuid 降级无此键。"""
    um = SimpleNamespace(uuid="u-1", content="帮我改一下登录页")
    um.__class__ = type("UserMessage", (), {})
    evs = normalize(um)
    assert evs == [{"kind": "user_msg", "uuid": "u-1", "text": "帮我改一下登录页"}]
    um2 = SimpleNamespace(content="没 uuid 的老消息")
    um2.__class__ = type("UserMessage", (), {})
    assert normalize(um2) == [{"kind": "user_msg", "uuid": "", "text": "没 uuid 的老消息"}]


def test_cancel_calls_client_interrupt_before_stopped():
    """取消时先 client.interrupt() 杀后台工具，再发 stopped 终态。"""
    calls = []
    class FakeClient:
        async def __aenter__(self): return self
        async def __aexit__(self, *a): return None
        async def query(self, p): pass
        async def receive_response(self):
            yield SimpleNamespace(content=[])
        async def interrupt(self): calls.append("interrupt")
    runner = ClaudeCodeRunner(client_factory=lambda *a, **k: FakeClient())
    events = []
    _run(runner.run("p", "/tmp", on_event=events.append, cancel_event=_AlwaysSet()))
    assert calls == ["interrupt"]
    assert events and events[-1]["kind"] == "stopped"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && uv run pytest tests/test_coding_plugin.py -k "checkpointing or user_msg or interrupt" -x -q`
Expected: FAIL

- [ ] **Step 3: 实现**

`plugins/coding/skills/_runner.py`：

① `_default_factory` 改：

```python
    def _default_factory(self, cwd: str, tools: list[str], resume: str | None = None,
                         permission_mode: str = "acceptEdits", can_use_tool=None):
        from claude_agent_sdk import ClaudeSDKClient, ClaudeAgentOptions  # lazy：测试不依赖真 SDK
        options = ClaudeAgentOptions(
            cwd=cwd, permission_mode=permission_mode, allowed_tools=tools, resume=resume,
            enable_file_checkpointing=True,            # rewind：CLI 侧文件检查点
            extra_args={"replay-user-messages": None},  # rewind：流中回 UserMessage（带 uuid 作回滚锚点）
            can_use_tool=can_use_tool,
        )
        return ClaudeSDKClient(options=options)
```

② `run` 签名与取消路径：

```python
    async def run(self, prompt: str, cwd: str, *, on_event, cancel_event,
                  resume_session_id: str | None = None,
                  permission_mode: str = "acceptEdits", can_use_tool=None) -> str | None:
        """……（docstring 补：permission_mode 透传 options；can_use_tool 透传权限回调；取消先 interrupt 再 stopped）"""
        factory = self._client_factory or self._default_factory
        cc_sid: str | None = None
        try:
            client = factory(cwd, self._allowed_tools, resume=resume_session_id,
                             permission_mode=permission_mode, can_use_tool=can_use_tool)
            async with client as c:
                await c.query(prompt)
                async for msg in c.receive_response():
                    if cancel_event.is_set():
                        # 先 interrupt 真杀后台工具（旧行为只停读，后台还在跑），再发 stopped 终态
                        interrupt = getattr(c, "interrupt", None)
                        if interrupt is not None:
                            try:
                                await interrupt()
                            except Exception:
                                pass
                        on_event({"kind": "stopped", "text": "已中断"})
                        return cc_sid
                    sid = getattr(msg, "session_id", None)
                    if sid:
                        cc_sid = sid
                    for ev in normalize(msg):
                        on_event(ev)
                        if ev.get("kind") == "done":
                            return cc_sid
            on_event({"kind": "done"})
            return cc_sid
        except Exception as e:
            print(f"[yibao/coding] runner 失败：{e}", file=sys.stderr)
            on_event({"kind": "error", "text": str(e)})
            return None
```

③ normalize User 分支（在 `_tool_result_events` 调用处）补 user_msg 提取：

```python
    if "User" in mtype:
        text = _user_text(msg)
        if text:
            return [{"kind": "user_msg", "uuid": str(getattr(msg, "uuid", "") or ""), "text": text}]
        return _tool_result_events(msg)
```

④ 新函数（`_tool_result_events` 旁）：

```python
def _user_text(msg: Any) -> str:
    """UserMessage 的纯文本提取（str content 或 text 块列表）；空 → 走 tool_result 分支。"""
    content = getattr(msg, "content", None)
    if isinstance(content, str):
        return content.strip()
    if isinstance(content, list):
        parts = [str(getattr(b, "text", "") or "") for b in content]
        return "\n".join(p for p in parts if p).strip()
    return ""
```

（注意与既有 `_tool_result_events` 的分工：先 `_user_text`，空才 `_tool_result_events`；tool_result 块的 text 提取别让它误判成 user_msg——`_user_text` 只取 `getattr(b,"text")`，ToolResultBlock 的 content 字段不是 text 属性故不误伤；实现时读 `_tool_result_events` 现状对齐。）

- [ ] **Step 4: 跑测试 + 全量回归**

Run: `cd sidecar && uv run pytest tests/test_coding_plugin.py -q && uv run pytest -q`
Expected: 全绿（既有 user_msg 无关用例不受 normalize 分支变化影响——tool_result 用例仍过）

- [ ] **Step 5: Commit**

```bash
git add plugins/coding/skills/_runner.py sidecar/tests/test_coding_plugin.py
git commit -m "feat(coding): 接线升级——checkpointing+replay 开启、取消真 interrupt、mode/can_use_tool 参数化、user_msg uuid 事件"
```

---

### Task 2: transcript 落库（messages 表 + 流式落库 + history 读库优先）

**Files:**
- Modify: `plugins/coding/manifest.toml`（sessions 加 mode 列 + messages 表）
- Modify: `plugins/coding/skills/coding.py`（_stream on_event 落库 + HistorySkill 读库优先）
- Test: `sidecar/tests/test_coding_plugin.py`（追加）

**Interfaces:**
- Consumes: `_stream.on_event`（coding.py:140-148）、HistorySkill（Task R1 后现状）
- Produces:
  - messages 表：`id pk/session_id/role/text/ts/seq/uuid default ""`（索引 session_id, seq）
  - `_stream` 落库：user prompt（role=user, text=prompt）+ text_delta 块（role=assistant）+ done/stopped（role=marker, text=完成/已中断）
  - `coding.history`：先读库（按 seq 正序 limit 40），空则 fallback `_cc_reader`

- [ ] **Step 1: 写失败测试**

`sidecar/tests/test_coding_plugin.py` 追加：

```python
def test_stream_persists_transcript_to_messages_table():
    """流式：user prompt + assistant 块 + 终态 marker 都落 messages 表（seq 单调）。"""
    ctx = _make_ctx_with_session(status="running")
    _run_fake_stream(ctx, events=[
        {"kind": "user_msg", "uuid": "u-1", "text": "任务一"},
        {"kind": "text_delta", "text": "好的"},
        {"kind": "done", "usage": {}},
    ])
    rows = sorted(ctx.db.query("messages", where={"session_id": "sid-1"}), key=lambda r: r["seq"])
    assert [(r["role"], r["text"]) for r in rows] == [
        ("user", "任务一"), ("assistant", "好的"), ("marker", "完成"),
    ]
    assert rows[0]["uuid"] == "u-1" and rows[1]["uuid"] == ""


def test_history_prefers_db_transcript_over_cc_reader(monkeypatch):
    """库里有 → 直接返回（不读 jsonl）；库里空 → fallback _cc_reader。"""
    ctx = _make_ctx_with_session(status="done", with_messages=[("user", "旧任务")])
    monkeypatch.setattr("_cc_reader.read_transcript", lambda *a, **k: (_ for _ in ()).throw(AssertionError("不该读 jsonl")))
    r = HistorySkill().run({"id": "sid-1"}, ctx)
    assert r.success and r.data["messages"] == [{"role": "user", "text": "旧任务", "uuid": ""}]
```

（`_run_fake_stream`/`_make_ctx_with_session(with_messages=...)` helper 名按文件惯例对齐；`_cc_reader` 的 monkeypatch 目标按 coding.py 里 `_sibling` 加载形态对齐——若 _sibling 缓存模块对象，patch 其函数属性。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && uv run pytest tests/test_coding_plugin.py -k "transcript or prefers_db" -x -q`
Expected: FAIL

- [ ] **Step 3: 实现**

① `plugins/coding/manifest.toml` sessions columns 加 `{name = "mode", type = "text", default = "acceptEdits"}`；追加表：

```toml
[[table]]
name = "messages"
columns = [
  {name = "id", type = "text", pk = true},
  {name = "session_id", type = "text"},
  {name = "role", type = "text"},        # user/assistant/marker
  {name = "text", type = "text"},
  {name = "ts", type = "integer"},
  {name = "seq", type = "integer"},
  {name = "uuid", type = "text", default = ""},   # user 消息的 CC uuid（rewind 回滚锚点）
]
indexes = ["session_id", "seq"]
```

② `plugins/coding/skills/coding.py` `_stream` 的 on_event 加落库（保持面板推送不变）：

```python
    seq = {"n": 0}

    def _persist(role: str, text: str, uuid: str = "") -> None:
        if not text:
            return
        seq["n"] += 1
        try:
            db.insert("messages", {
                "session_id": sid, "role": role, "text": text,
                "ts": int(time.time()), "seq": seq["n"], "uuid": uuid,
            })
        except Exception as e:
            print(f"[yibao/coding] transcript 落库失败（跳过）：{e}", file=sys.stderr)

    def on_event(ev: dict) -> None:
        kind = ev.get("kind")
        if kind == "user_msg":
            _persist("user", str(ev.get("text") or ""), str(ev.get("uuid") or ""))
        elif kind == "text_delta":
            _persist("assistant", str(ev.get("text") or ""))
        elif kind == "done":
            _persist("marker", "完成")
        elif kind == "stopped":
            _persist("marker", str(ev.get("text") or "已中断"))
        if emit_event is not None:
            …（原 panel_data 推送不变）…
        if ev.get("kind") == "error":
            state["error"] = True
```

（首条 user prompt 由 runner 的 user_msg 事件提供；resume 轮同理。若某轮 SDK 未 replay 出 user_msg，prompt 不落库——可接受，不补假数据。）

③ HistorySkill.run 改读库优先：

```python
        cc = row.get("cc_session_id") or ""
        rows = ctx.db.query("messages", where={"session_id": sid}, order="seq", limit=40)
        if rows:
            messages = [{"role": r["role"], "text": r["text"], "uuid": r.get("uuid") or ""} for r in rows]
        else:
            reader = _sibling("_cc_reader")
            messages = reader.read_transcript(cc, limit=40) if cc else []
```

- [ ] **Step 4: 跑测试 + 全量回归**

Run: `cd sidecar && uv run pytest tests/test_coding_plugin.py -q && uv run pytest -q`
Expected: 全绿

- [ ] **Step 5: Commit**

```bash
git add plugins/coding/manifest.toml plugins/coding/skills/coding.py sidecar/tests/test_coding_plugin.py
git commit -m "feat(coding): transcript 落库——messages 表 + 流式持久化（含 user uuid）+ history 读库优先"
```

---

### Task 3: plan mode 切换（db mode + runner 透传 + 运行中 set_permission_mode + 面板 pill）

**Files:**
- Modify: `plugins/coding/skills/coding.py`（StartSkill/SendSkill 透传 mode、ModeSkill、_SESSIONS mode_pending）
- Modify: `plugins/coding/skills/_runner.py`（消息前检查 mode_pending → set_permission_mode）
- Modify: `plugins/coding/api.toml`（mode quiet 条目）
- Modify: `plugins/coding/panel/chat.html`（prompt 区 mode pill + 切换处理）
- Test: `sidecar/tests/test_coding_plugin.py`（追加）

**Interfaces:**
- Consumes: Task 1 的 permission_mode 参数；sessions.mode 列（Task 2 已加）
- Produces:
  - api `coding.mode {id, mode} -> {ok, mode, live}`（direct+quiet，L1）
  - `_SESSIONS[sid]["mode_pending"]` → runner 每条消息前 `client.set_permission_mode(mode)`（鸭子类型，失败静默）
  - 面板 pill 两态：`自动改文件`（acceptEdits）⇄ `只读规划`（plan）

- [ ] **Step 1: 写失败测试**

```python
def test_start_and_send_persist_mode():
    ctx = _make_ctx_empty()
    StartSkill().run({"cwd": "/tmp", "prompt": "x", "mode": "plan"}, ctx)
    row = ctx.db.query("sessions", where={})[0] if False else ctx.db.query("sessions", where={"id": ctx.last_sid})[0]
    assert row["mode"] == "plan"
    # send 不带 mode → 沿用库里的；带 → 覆盖
    ...


def test_mode_skill_updates_db_and_live_pending():
    ctx = _make_ctx_with_session(status="running")
    r = ModeSkill().run({"id": "sid-1", "mode": "plan"}, ctx)
    assert r.success and r.data["live"] is True   # 有 live entry → mode_pending 已置
    assert ctx.sessions_registry["sid-1"]["mode_pending"] == "plan"
    assert ctx.db.query("sessions", where={"id": "sid-1"})[0]["mode"] == "plan"


def test_runner_applies_pending_mode_between_messages():
    """runner 每条消息前检查 mode_pending → 调 client.set_permission_mode。"""
    ...
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && uv run pytest tests/test_coding_plugin.py -k mode -x -q`
Expected: FAIL

- [ ] **Step 3: 实现**

① `plugins/coding/skills/coding.py`：
- StartSkill.run：`mode = str(params.get("mode") or "acceptEdits").strip() or "acceptEdits"`；`start_session(..., mode=mode)`（start_session 纯函数加 mode 参数落列）；`_spawn_stream(..., permission_mode=mode)`。
- SendSkill.run：`mode = str(params.get("mode") or row.get("mode") or "acceptEdits")`；db.update 回写 mode；`_spawn_stream(..., permission_mode=mode)`。
- `_spawn_stream`/`_stream` 加 `permission_mode` 透传参数。
- 新 ModeSkill：

```python
class ModeSkill(Skill):
    id = "coding.mode"
    label = "切换权限模式"
    description = "切换某个 coding 会话的权限模式（acceptEdits=自动改文件 / plan=只读规划）：落库下轮生效；会话在跑则通知 runner 运行中切换。"
    default_risk = RiskLevel.L1_LOW

    def openai_schema(self) -> dict:
        return {"type": "function", "function": {"name": self.id, "description": self.description,
                "parameters": {"type": "object",
                    "properties": {"id": {"type": "string"}, "mode": {"type": "string"}},
                    "required": ["id", "mode"]}}}

    def run(self, params: dict, ctx: Any) -> ActionResult:
        sid = str(params.get("id") or "").strip()
        mode = str(params.get("mode") or "").strip()
        if mode not in ("acceptEdits", "plan"):
            return ActionResult(success=False, error=f"不支持的模式：{mode}（仅 acceptEdits/plan）")
        rows = ctx.db.query("sessions", where={"id": sid})
        if not rows:
            return ActionResult(success=False, error=f"会话不存在：{sid}")
        ctx.db.update("sessions", sid, {"mode": mode})
        live = sid in _SESSIONS
        if live:
            _SESSIONS[sid]["mode_pending"] = mode
        return ActionResult(success=True, data={"ok": True, "mode": mode, "live": live})
```

② `plugins/coding/skills/_runner.py` run 的消息循环内（cancel 检查之后）加：

```python
                    # 运行中模式切换（coding.mode 写入 _SESSIONS mode_pending；下条消息生效，延迟 ≤1 条）
                    entry = _session_registry_get(cc_sid_holder[0]) if False else None
```

——实际实现：run 加参数 `session_entry: dict | None = None`（由 _spawn_stream 传 `_SESSIONS[sid]`），循环内：

```python
                    if session_entry is not None:
                        pending = session_entry.pop("mode_pending", None)
                        if pending is not None:
                            set_mode = getattr(c, "set_permission_mode", None)
                            if set_mode is not None:
                                try:
                                    await set_mode(pending)
                                except Exception as e:
                                    print(f"[yibao/coding] 运行中切换模式失败（已跳过）：{e}", file=sys.stderr)
```

③ `plugins/coding/api.toml` 追加：

```toml
# 面板 mode pill：切换权限模式（落库 + 运行中切换；quiet 不发 panel 事件）
[[method]]
name = "mode"
handler = "coding.mode"
direct = true
quiet = true
```

④ `plugins/coding/panel/chat.html`：prompt 区（footer，:297 附近）加 pill：

```html
<button id="mode-pill" class="pill" title="点击切换权限模式">自动改文件</button>
```

```js
  var curMode = "acceptEdits"; // acceptEdits=自动改文件；plan=只读规划
  function renderModePill() { $("mode-pill").textContent = curMode === "plan" ? "只读规划" : "自动改文件"; $("mode-pill").classList.toggle("plan", curMode === "plan"); }
  $("mode-pill").addEventListener("click", function () {
    curMode = curMode === "plan" ? "acceptEdits" : "plan";
    renderModePill();
    if (currentSession) window.yibao.invoke("coding.mode", { id: currentSession, mode: curMode }).catch(function () {});
  });
```

send() 的 params 加 `mode: curMode`（start 与 send 都带）。pill 样式：`.pill{font-size:11px;padding:2px 10px;border-radius:999px;border:1px solid var(--border-strong);background:var(--surface-2);cursor:pointer} .pill.plan{border-color:var(--accent);color:var(--accent-deep)}`。

- [ ] **Step 4: 跑测试 + 全量回归 + 构建**

Run: `cd sidecar && uv run pytest -q && cd ../app && npx vite build`
Expected: 全绿

- [ ] **Step 5: Commit**

```bash
git add plugins/coding/skills/coding.py plugins/coding/skills/_runner.py plugins/coding/api.toml plugins/coding/panel/chat.html sidecar/tests/test_coding_plugin.py
git commit -m "feat(coding): plan mode 切换——pill 两态 + 落库透传 + 运行中 set_permission_mode"
```

---

### Task 4: rewind 检查点回滚（面板 ⏪ + coding.rewind）

**Files:**
- Modify: `plugins/coding/skills/coding.py`（RewindSkill + live rewind_pending）
- Modify: `plugins/coding/skills/_runner.py`（消息前检查 rewind_pending → rewind_files）
- Modify: `plugins/coding/api.toml`（rewind quiet 条目）
- Modify: `plugins/coding/panel/chat.html`（用户气泡 ⏪ 按钮 + rewind_ok 事件渲染）
- Test: `sidecar/tests/test_coding_plugin.py`（追加）

**Interfaces:**
- Consumes: messages 表 user 行 uuid（Task 2）、`_SESSIONS[sid]` entry（mode_pending 同款机制）、`client.rewind_files(user_message_id)`
- Produces:
  - api `coding.rewind {id, user_msg_id} -> {ok, live}`（direct+quiet，L1）
  - 事件 `{"kind":"rewind_ok","text":"已回滚到此前的文件状态"}` / 失败 `{"kind":"error","text":"回滚失败：…"}`

- [ ] **Step 1: 写失败测试**

```python
def test_rewind_live_session_defers_to_runner():
    """会话在跑：rewind 只写 rewind_pending，runner 下条消息前执行 rewind_files。"""
    ctx = _make_ctx_with_session(status="running")
    ctx.sessions_registry["sid-1"] = {"cancel": _NeverSet()}
    r = RewindSkill().run({"id": "sid-1", "user_msg_id": "u-1"}, ctx)
    assert r.success and r.data["live"] is True
    assert ctx.sessions_registry["sid-1"]["rewind_pending"] == "u-1"


def test_rewind_idle_session_uses_fresh_client():
    """会话已结束：新开 client（resume=cc_session_id）connect → rewind_files → disconnect。"""
    ...
    assert client.calls == ["connect", ("rewind_files", "u-1"), "disconnect"]


def test_rewind_failure_degrades():
    """无 cc_session_id 且不在跑 → 失败文案，不炸。"""
    ...
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && uv run pytest tests/test_coding_plugin.py -k rewind -x -q`
Expected: FAIL

- [ ] **Step 3: 实现**

① `plugins/coding/skills/coding.py` RewindSkill：

```python
def _rewind_fresh_client(cc_sid: str, cwd: str, uuid: str) -> None:
    """非 live 路径：新开 client（resume + checkpointing on）执行 rewind_files。CLI 侧 checkpoint 持久，跨实例应可；
    失败抛给调用方（RewindSkill 降级成 error 事件）。"""
    runner = ClaudeCodeRunner()
    factory = runner._default_factory  # 复用 options（checkpointing/replay 已开）

    async def _do() -> None:
        client = factory(cwd, ["Read", "Write", "Edit", "MultiEdit", "Bash", "Glob", "Grep"], resume=cc_sid)
        await client.connect()
        try:
            await client.rewind_files(uuid)
        finally:
            await client.disconnect()

    asyncio.new_event_loop().run_until_complete(_do())


class RewindSkill(Skill):
    id = "coding.rewind"
    label = "回滚文件检查点"
    description = "把会话改过的文件回滚到某条用户消息时的状态（Claude Code 文件检查点）。会话在跑则下条消息前执行；已结束则新开 client 执行。"
    default_risk = RiskLevel.L1_LOW

    def openai_schema(self) -> dict:
        return {"type": "function", "function": {"name": self.id, "description": self.description,
                "parameters": {"type": "object",
                    "properties": {"id": {"type": "string"}, "user_msg_id": {"type": "string"}},
                    "required": ["id", "user_msg_id"]}}}

    def run(self, params: dict, ctx: Any) -> ActionResult:
        sid = str(params.get("id") or "").strip()
        uuid = str(params.get("user_msg_id") or "").strip()
        if not uuid:
            return ActionResult(success=False, error="缺少回滚锚点 user_msg_id")
        rows = ctx.db.query("sessions", where={"id": sid})
        if not rows:
            return ActionResult(success=False, error=f"会话不存在：{sid}")
        row = rows[0]
        emit = getattr(ctx, "emit_event", None)

        def _emit(ev: dict) -> None:
            if emit is not None:
                emit({"kind": "panel_data", "payload": {"panel": "coding:chat",
                      "data": {"session_id": sid, "event": ev}}})

        if sid in _SESSIONS:  # 在跑：下条消息前执行（runner 消费 rewind_pending）
            _SESSIONS[sid]["rewind_pending"] = uuid
            return ActionResult(success=True, data={"ok": True, "live": True})
        cc = row.get("cc_session_id") or ""
        if not cc:
            return ActionResult(success=False, error="该会话无检查点可回滚（cc_session_id 为空）")
        try:
            _rewind_fresh_client(cc, row.get("cwd") or "", uuid)
        except Exception as e:
            _emit({"kind": "error", "text": f"回滚失败：{e}"})
            return ActionResult(success=False, error=f"回滚失败：{e}")
        _emit({"kind": "rewind_ok", "text": "已回滚到此前的文件状态"})
        return ActionResult(success=True, data={"ok": True, "live": False})
```

② `plugins/coding/skills/_runner.py` 消息循环内（mode_pending 检查同处）加：

```python
                    if session_entry is not None:
                        rew = session_entry.pop("rewind_pending", None)
                        if rew is not None:
                            rewind_files = getattr(c, "rewind_files", None)
                            if rewind_files is not None:
                                try:
                                    await rewind_files(rew)
                                    on_event({"kind": "rewind_ok", "text": "已回滚到此前的文件状态"})
                                except Exception as e:
                                    on_event({"kind": "error", "text": f"回滚失败：{e}"})
```

③ `plugins/coding/api.toml` 追加：

```toml
# 用户气泡 ⏪：文件检查点回滚（quiet 不发 panel 事件）
[[method]]
name = "rewind"
handler = "coding.rewind"
direct = true
quiet = true
```

④ `plugins/coding/panel/chat.html`：
- appendUser 签名加可选 uuid 参数；`handleInitData` 加 `case "user_msg"`：若文本与最后一条用户输入一致（或简单：直接渲染带 uuid 的用户气泡——注意与 send() 里 appendUser(prompt) 可能重复：send() 已先画用户气泡，user_msg 事件再画会双份。**处理：send() 不再直接 appendUser，改等 user_msg 事件来画（uuid 锚点）；user_msg 长时间不到（老 SDK/失败）则 1.5s 后兜底 appendUser 无 ⏪**——实现时选这个防双份方案）。
- 用户气泡右上 ⏪ 按钮（有 uuid 才渲染）：

```js
  function appendUser(text, uuid) {
    var row = document.createElement("div");
    row.className = "row user";
    var b = document.createElement("div");
    b.className = "bubble";
    b.textContent = text;
    if (uuid) {
      var rw = document.createElement("button");
      rw.className = "rewind-btn";
      rw.title = "回滚到这条消息时的文件状态";
      rw.textContent = "⏪";
      rw.addEventListener("click", function (e) {
        e.stopPropagation();
        if (!currentSession) return;
        rw.disabled = true;
        window.yibao.invoke("coding.rewind", { id: currentSession, user_msg_id: uuid }).then(function (r) {
          setStatus(r && r.ok === false ? ("回滚失败：" + (r.error || "")) : "已提交回滚…", !!(r && r.ok === false));
        }).catch(function (err) {
          setStatus("回滚失败：" + emsg(err), true);
        }).finally(function () { rw.disabled = false; });
      });
      b.appendChild(rw);
    }
    row.appendChild(b);
    $("log").appendChild(row);
    scrollIfAtBottom();
  }
```

- `handleInitData` 加：`case "rewind_ok": appendMarker(ev.text || "已回滚", false); break;`

- [ ] **Step 4: 跑测试 + 全量回归 + 构建**

Run: `cd sidecar && uv run pytest -q && cd ../app && npx vite build`
Expected: 全绿

- [ ] **Step 5: Commit**

```bash
git add plugins/coding/skills/coding.py plugins/coding/skills/_runner.py plugins/coding/api.toml plugins/coding/panel/chat.html sidecar/tests/test_coding_plugin.py
git commit -m "feat(coding): rewind 检查点回滚——用户气泡 ⏪ + live/新 client 两路 rewind_files"
```

---

### Task 5: can_use_tool 权限交互（回调桥 + 审批卡 + coding.decide）

**Files:**
- Modify: `plugins/coding/skills/_runner.py`（can_use_tool 回调 + _DECISIONS 桥）
- Modify: `plugins/coding/skills/coding.py`（DecideSkill + _spawn_stream 挂回调）
- Modify: `plugins/coding/api.toml`（decide quiet 条目）
- Modify: `plugins/coding/panel/chat.html`（审批卡渲染 + 按钮）
- Test: `sidecar/tests/test_coding_plugin.py`（追加）

**Interfaces:**
- Consumes: SDK `can_use_tool` 回调（async (tool_name, input, context) → PermissionResultAllow/Deny）
- Produces:
  - `_PERM = {}`（rid → {"event": threading.Event, "allow": bool|None}）
  - 事件 `{"kind":"permission_request","rid","tool","input"}`（面板流）与 `{"kind":"permission_done","rid","allow"}`
  - api `coding.decide {rid, allow} -> {ok}`（direct+quiet，L1）
  - 超时 60s 默认 deny「超时未批准」

- [ ] **Step 1: 写失败测试**

```python
def test_can_use_tool_roundtrip_approve_and_deny():
    """回调发 permission_request 并等待；decide(allow=True) → PermissionResultAllow；decide(False) → Deny。"""
    ...


def test_can_use_tool_timeout_defaults_deny():
    """60s（测试注入短超时）无裁决 → Deny('超时未批准')。"""
    ...


def test_decide_skill_resolves_pending():
    r = DecideSkill().run({"rid": "perm_1", "allow": True}, ctx)
    assert r.success
    assert _PERM["perm_1"]["allow"] is True and _PERM["perm_1"]["event"].is_set()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && uv run pytest tests/test_coding_plugin.py -k "can_use_tool or decide" -x -q`
Expected: FAIL

- [ ] **Step 3: 实现**

① `plugins/coding/skills/_runner.py` 顶部加注册表与工厂：

```python
import threading, itertools

_PERM: dict = {}
_perm_seq = itertools.count(1)


def make_permission_callback(sid: str, on_event, *, timeout_s: float = 60.0):
    """can_use_tool 回调桥：向面板发 permission_request，等 coding.decide 裁决（超时默认 deny）。
    回调本体与事件任何异常 → deny（安全默认）+ stderr。返回 SDK 期望的 async callable。"""
    async def _cb(tool_name, input, context=None):
        rid = f"perm_{sid}_{next(_perm_seq)}"
        entry = {"event": threading.Event(), "allow": None}
        _PERM[rid] = entry
        try:
            on_event({"kind": "permission_request", "rid": rid,
                      "tool": str(tool_name), "input": input if isinstance(input, dict) else {}})
        except Exception as e:
            print(f"[yibao/coding] 权限请求事件发送失败（deny）：{e}", file=sys.stderr)
            _PERM.pop(rid, None)
            return _deny(f"请求发送失败：{e}")
        got = await asyncio.to_thread(entry["event"].wait, timeout_s)
        allow = entry["allow"] if got else None
        _PERM.pop(rid, None)
        if allow is True:
            on_event({"kind": "permission_done", "rid": rid, "allow": True})
            return _allow()
        on_event({"kind": "permission_done", "rid": rid, "allow": False})
        return _deny("用户拒绝" if allow is False else "超时未批准")
    return _cb


def _allow():
    from claude_agent_sdk import PermissionResultAllow
    return PermissionResultAllow()


def _deny(message: str):
    from claude_agent_sdk import PermissionResultDeny
    return PermissionResultDeny(message=message)
```

（`asyncio` 模块若未 import 则补；测试注入 `timeout_s` 小值；`_allow/_deny` lazy import 防测试环境无 SDK 类型——若测试环境有可直接顶层 import。）

② `plugins/coding/skills/coding.py`：
- `_spawn_stream` 创建 runner 后传 `can_use_tool=make_permission_callback(sid, on_event 内部引用)`——注意回调需要 on_event：`_stream` 内定义 on_event 后再建 runner？现状 runner 由调用方（StartSkill/SendSkill）创建传入。**改法：_spawn_stream 接 runner 并在调 runner.run 时传 can_use_tool=make_permission_callback(sid, on_event)**——on_event 在 _stream 内，run 的参数由 _stream 传（Task 1 已把 run 参数扩了 can_use_tool，_stream 调用点补上即可）。
- DecideSkill：

```python
class DecideSkill(Skill):
    id = "coding.decide"
    label = "裁决工具权限"
    description = "对 can_use_tool 弹出的权限请求做允许/拒绝裁决（rid 来自 permission_request 事件）。"
    default_risk = RiskLevel.L1_LOW

    def openai_schema(self) -> dict:
        return {"type": "function", "function": {"name": self.id, "description": self.description,
                "parameters": {"type": "object",
                    "properties": {"rid": {"type": "string"}, "allow": {"type": "boolean"}},
                    "required": ["rid", "allow"]}}}

    def run(self, params: dict, ctx: Any) -> ActionResult:
        rid = str(params.get("rid") or "").strip()
        allow = bool(params.get("allow"))
        entry = _PERM.get(rid)
        if entry is None:
            return ActionResult(success=False, error="权限请求不存在或已超时")
        entry["allow"] = allow
        entry["event"].set()
        return ActionResult(success=True, data={"ok": True})
```

（`_PERM`/`make_permission_callback` 自 `_sibling("_runner")` 引用，注意模块单例一致性——`_sibling` 加载即同一模块对象，注册表共享。）

③ `plugins/coding/api.toml` 追加：

```toml
# 权限审批卡按钮：允许/拒绝（quiet 不发 panel 事件）
[[method]]
name = "decide"
handler = "coding.decide"
direct = true
quiet = true
```

④ `plugins/coding/panel/chat.html` handleInitData 加两 case：

```js
      case "permission_request":
        currentBubble = null;
        appendPermission(ev);
        break;
      case "permission_done": {
        var card = document.getElementById("perm-" + ev.rid);
        if (card) {
          card.classList.add(ev.allow ? "ok" : "no");
          var btns = card.querySelectorAll("button");
          for (var i = 0; i < btns.length; i++) btns[i].disabled = true;
          card.querySelector(".perm-state").textContent = ev.allow ? "已允许" : "已拒绝";
        }
        break;
      }
```

`appendPermission`：

```js
  function appendPermission(ev) {
    var card = document.createElement("div");
    card.className = "card perm-card";
    card.id = "perm-" + ev.rid;
    var inputStr = "";
    try { inputStr = JSON.stringify(ev.input || {}); } catch (e) { inputStr = String(ev.input); }
    if (inputStr.length > 300) inputStr = inputStr.slice(0, 300) + "…";
    card.innerHTML = "<div class=\"perm-head\">🔐 工具「" + esc(ev.tool) + "」请求权限</div>" +
      "<pre class=\"perm-input\">" + esc(inputStr) + "</pre>" +
      "<div class=\"perm-actions\"><button class=\"perm-no\">拒绝</button><button class=\"perm-ok\">允许</button></div>" +
      "<div class=\"perm-state\">等待批准（60s 后默认拒绝）</div>";
    card.querySelector(".perm-ok").addEventListener("click", function () { decidePerm(ev.rid, true); });
    card.querySelector(".perm-no").addEventListener("click", function () { decidePerm(ev.rid, false); });
    $("log").appendChild(card);
    scrollIfAtBottom();
  }

  function decidePerm(rid, allow) {
    window.yibao.invoke("coding.decide", { rid: rid, allow: allow }).catch(function (e) {
      setStatus("裁决失败：" + emsg(e), true);
    });
  }
```

样式补：`.perm-card{border-color:var(--warn,#b45309)} .perm-head{font-weight:600} .perm-input{max-height:120px;overflow:auto;font-size:11px} .perm-actions{display:flex;gap:8px;margin:6px 0} .perm-ok{background:#16a34a;color:#fff} .perm-no{background:#dc2626;color:#fff} .perm-card.ok{border-color:#16a34a} .perm-card.no{border-color:#dc2626} .perm-state{font-size:11px;opacity:.7}`。

- [ ] **Step 4: 跑测试 + 全量回归 + 构建**

Run: `cd sidecar && uv run pytest -q && cd ../app && npx vite build`
Expected: 全绿

- [ ] **Step 5: Commit**

```bash
git add plugins/coding/skills/_runner.py plugins/coding/skills/coding.py plugins/coding/api.toml plugins/coding/panel/chat.html sidecar/tests/test_coding_plugin.py
git commit -m "feat(coding): can_use_tool 权限交互——回调桥 + 审批卡 + coding.decide（超时默认 deny）"
```

---

### Task 6: @files 上下文（coding.files + @ 自动补全）

**Files:**
- Modify: `plugins/coding/skills/coding.py`（FilesSkill）
- Modify: `plugins/coding/api.toml`（files quiet 条目）
- Modify: `plugins/coding/panel/chat.html`（textarea @ 补全）
- Test: `sidecar/tests/test_coding_plugin.py`（追加）

**Interfaces:**
- Consumes: cwd（药丸值）
- Produces: api `coding.files {cwd, q} -> {files: [{path, rel}]}`（direct+quiet，L0）

- [ ] **Step 1: 写失败测试**

```python
def test_files_fuzzy_match_and_excludes(tmp_path):
    (tmp_path / "src").mkdir()
    (tmp_path / "src" / "login.ts").write_text("x")
    (tmp_path / "node_modules").mkdir()
    (tmp_path / "node_modules" / "junk.js").write_text("x")
    r = FilesSkill().run({"cwd": str(tmp_path), "q": "login"}, _ctx())
    files = r.data["files"]
    assert any(f["rel"] == "src/login.ts" for f in files)
    assert not any("node_modules" in f["rel"] for f in files)


def test_files_caps_results_and_bad_cwd(tmp_path):
    r = FilesSkill().run({"cwd": str(tmp_path / "ghost"), "q": ""}, _ctx())
    assert r.success and r.data["files"] == []
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && uv run pytest tests/test_coding_plugin.py -k files -x -q`
Expected: FAIL

- [ ] **Step 3: 实现**

① `plugins/coding/skills/coding.py` FilesSkill：

```python
_FILES_EXCLUDE = {".git", "node_modules", "dist", "target", ".venv", "build", "out", "__pycache__", ".next", ".cache"}


class FilesSkill(Skill):
    id = "coding.files"
    label = "项目文件模糊搜索"
    description = "在 cwd 下按文件名模糊匹配（@ 补全用）：限深 6 层、限 200 条、排除依赖/构建目录。"
    default_risk = RiskLevel.L0_READONLY

    def openai_schema(self) -> dict:
        return {"type": "function", "function": {"name": self.id, "description": self.description,
                "parameters": {"type": "object",
                    "properties": {"cwd": {"type": "string"}, "q": {"type": "string"}},
                    "required": ["cwd"]}}}

    def run(self, params: dict, ctx: Any) -> ActionResult:
        cwd = os.path.expanduser(str(params.get("cwd") or "").strip())
        q = str(params.get("q") or "").strip().lower()
        out: list[dict] = []
        if not os.path.isdir(cwd):
            return ActionResult(success=True, data={"files": []})
        try:
            for root, dirs, files in os.walk(cwd):
                rel_root = os.path.relpath(root, cwd)
                depth = 0 if rel_root == "." else rel_root.count(os.sep) + 1
                dirs[:] = [d for d in dirs if d not in _FILES_EXCLUDE and not d.startswith(".")]
                if depth >= 6:
                    dirs[:] = []
                for name in files:
                    if name.startswith("."):
                        continue
                    rel = name if rel_root == "." else f"{rel_root}/{name}"
                    if q and q not in rel.lower():
                        continue
                    out.append({"path": os.path.join(root, name), "rel": rel})
                    if len(out) >= 200:
                        return ActionResult(success=True, data={"files": out})
        except Exception as e:
            print(f"[yibao/coding] files 遍历失败（截断返回）：{e}", file=sys.stderr)
        return ActionResult(success=True, data={"files": out})
```

② `plugins/coding/api.toml` 追加：

```toml
# @ 补全：项目文件模糊搜索（quiet 不发 panel 事件）
[[method]]
name = "files"
handler = "coding.files"
direct = true
quiet = true
```

③ `plugins/coding/panel/chat.html`（textarea #prompt 加 @ 下拉）：

```js
  // ---- @ 补全：输入 @ 起模糊搜项目文件，↑↓ 选择，Enter 插入，Esc 关闭 ----
  var atState = { open: false, items: [], idx: 0, query: "", start: -1 };
  function closeAt() { atState.open = false; $("at-menu").hidden = true; }
  function openAt() { atState.open = true; $("at-menu").hidden = false; }

  function atQuery() {
    if (!hasBridge) return;
    var cwd = $("cwd").value.trim();
    if (!cwd) return;
    window.yibao.invoke("coding.files", { cwd: cwd, q: atState.query }).then(function (r) {
      atState.items = (r && r.files) || [];
      atState.idx = 0;
      var m = $("at-menu");
      m.innerHTML = "";
      if (!atState.items.length) { m.innerHTML = "<div class=\"at-empty\">无匹配</div>"; openAt(); return; }
      atState.items.slice(0, 12).forEach(function (f, i) {
        var d = document.createElement("div");
        d.className = "at-item" + (i === 0 ? " sel" : "");
        d.textContent = f.rel;
        d.addEventListener("mousedown", function (e) { e.preventDefault(); atInsert(f.rel); });
        m.appendChild(d);
      });
      openAt();
    }).catch(function () { closeAt(); });
  }

  function atInsert(rel) {
    var ta = $("prompt");
    var v = ta.value;
    ta.value = v.slice(0, atState.start) + "@" + rel + " " + v.slice(ta.selectionStart);
    ta.focus();
    closeAt();
  }

  $("prompt").addEventListener("input", function () {
    var ta = this;
    var pos = ta.selectionStart;
    var before = ta.value.slice(0, pos);
    var m = before.match(/@([\w\-./]*)$/);
    if (m) {
      atState.start = pos - m[0].length;
      atState.query = m[1];
      atQuery();
    } else if (atState.open) closeAt();
  });
  $("prompt").addEventListener("keydown", function (e) {
    if (!atState.open) return;
    if (e.key === "Escape") { closeAt(); e.stopPropagation(); }
    else if (e.key === "ArrowDown" || e.key === "ArrowUp") {
      e.preventDefault();
      var items = document.querySelectorAll("#at-menu .at-item");
      if (!items.length) return;
      items[atState.idx].classList.remove("sel");
      atState.idx = (atState.idx + (e.key === "ArrowDown" ? 1 : -1) + items.length) % items.length;
      items[atState.idx].classList.add("sel");
    } else if (e.key === "Enter" && atState.items.length) {
      e.preventDefault(); e.stopPropagation();
      atInsert(atState.items[atState.idx].rel);
    }
  }, true);
```

DOM：textarea 容器相对定位，加 `<div id="at-menu" class="at-menu" hidden></div>`；样式：`.at-menu{position:absolute;bottom:100%;left:0;right:0;max-height:200px;overflow:auto;background:var(--card);border:1px solid var(--border);border-radius:var(--r-md);box-shadow:var(--shadow-card);z-index:30} .at-item{padding:6px 10px;font-family:var(--mono);font-size:12px;cursor:pointer} .at-item.sel{background:var(--surface-2)} .at-empty{padding:8px;opacity:.6;font-size:12px}`。

- [ ] **Step 4: 跑测试 + 全量回归 + 构建**

Run: `cd sidecar && uv run pytest -q && cd ../app && npx vite build`
Expected: 全绿

- [ ] **Step 5: Commit**

```bash
git add plugins/coding/skills/coding.py plugins/coding/api.toml plugins/coding/panel/chat.html sidecar/tests/test_coding_plugin.py
git commit -m "feat(coding): @files 上下文——coding.files 模糊搜索 + textarea @ 自动补全"
```

---

### Task 7: 统一验收（第一轮 + 第二轮全量）

- [ ] **Step 1: 自动化全绿**

```bash
cd sidecar && uv run pytest -q
cd ../app && npx vue-tsc --noEmit && npx vite build && cargo check --manifest-path src-tauri/Cargo.toml
```

- [ ] **Step 2: 真机（人工）统一验收清单**

第一轮（回归）：①起会话看 thinking/结果/diff/秒表 ②流式中发送禁用、中断即复位 ③完成行耗时 token ④历史抽屉恢复接着聊 ⑤📁 选目录 ⑥steer 引导
第二轮（新能力）：
7. **plan mode**：pill 切「只读规划」→ 让它改文件 → 它只出方案不动文件；运行中切回「自动改文件」→ 下轮生效
8. **rewind**：让它改一个文件 → 点那条用户消息气泡的 ⏪ → 文件内容回到改动前（git diff 验证）
9. **权限审批**：触发一条 Bash（如「跑一下 ls」）→ 弹审批卡 → 拒绝看被拒/允许看执行；超时 60s 默认拒绝（可只验快速路径）
10. **@files**：输入 @ → 下拉匹配 → 插入 @src/login.ts → 发送后它真读了该文件
11. **transcript**：重开面板 → 历史抽屉恢复 → 历史来自 messages 表（速度感知 + 内容完整）

- [ ] **Step 3: 收尾 commit（如有验收修小补）**

```bash
git add -p
git commit -m "fix(coding): R2 统一验收小修"
```

---

## 自审

- spec 覆盖：R1 接线（T1）transcript（T2）plan mode（T3）rewind（T4）can_use_tool（T5）@files（T6）✅；停止强化 interrupt 在 T1 ✅
- 类型一致：`user_msg{uuid,text}`（T1 产=T2 落库=T4 锚点）✅；`mode_pending`/`rewind_pending`（T3/T4 同机制）✅；`permission_request{rid,tool,input}`/`permission_done{rid,allow}`（T5 runner↔面板↔DecideSkill）✅；`coding.mode/rewind/decide/files`（api.toml=面板 invoke=Skill id）✅
- 无占位：后端全量代码 + 面板新函数全量代码 ✅
