# v1.1 Slice 0（造尺子：度量基座）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 v1.1 收口迭代建度量基座——信任统计读模型（feed 聚合 → `feed_stats` IPC → 设置页统计块）、watch 主动行为验收测试、TTS 三引擎验收脚本、点击评测场景采集。

**Architecture:** 四条独立工作流：A) `FeedStore.stats()` SQL 聚合 → server `feed_stats` 命令 → Rust/brain.ts → SettingsView 只读统计块（Task 1/2/5，有依赖顺序）；B) watch 主动行为经 ProactiveDispatcher 落真实 FeedStore 的验收测试（Task 3）；C) `scripts/eval_tts.py` 验收脚本，纯判定逻辑进 pytest，真机手动跑（Task 4）；D) 点击场景采集为手动任务（Task 6）。

**Tech Stack:** Python 3（sidecar，stdlib sqlite3/resource）、pytest、Tauri v2（Rust）、Vue 3 + TS。

**Spec:** `docs/superpowers/specs/2026-08-01-v1.1-consolidation-design.md`（§3 达标线是验收契约，本 Slice 只造尺子不定案）。

## Global Constraints

- sidecar 测试：`cd sidecar && uv run --extra dev pytest -q`（venv 默认不含 pytest，必须带 `--extra dev`）。
- eval 脚本（eval_click/eval_tts）手动跑、非 CI；报告目录 `sidecar/scripts/eval_reports/` 不入库（加 .gitignore）。
- Feed 是增强面：所有读路径失败只 print 降级、不抛（feed.py 模块 docstring 原则）。
- 提交信息：中文 conventional commit（如 `feat(feed): …`），每任务一提交。
- 前端验证：`cd app && npm run build`（含 vue-tsc 类型检查）；app 无 vitest，纯展示块走类型检查 + 人工目验。
- Rust 验证：`cd app/src-tauri && cargo check`。
- 已有覆盖不重复造：HealthNudge/ProactiveChat/Budget 单测见 `sidecar/tests/test_watch.py`（31 例）；dispatcher gating 见 `test_proactive.py`。

---

### Task 1: FeedStore.stats() 信任统计读模型 + 文档首提

**Files:**
- Modify: `sidecar/src/yibao_brain/feed.py`（在 `count_ignored` 之后、`close` 之前加方法）
- Test: `sidecar/tests/test_feed.py`

**Interfaces:**
- Produces: `FeedStore.stats(days: int = 7) -> dict`，返回：
  ```python
  {
    "days": int, "since": float, "total": int,
    "by_kind": {"task": int, "reminder": int, "event": int},   # 固定三键（_KINDS）
    "by_day": [{"day": "YYYY-MM-DD", "kind": str, "count": int}],  # 本地时区按天，倒序
    "read_rate": float,      # 0..1，保留 4 位；total=0 时 0.0
    "ignored_rate": float,   # 同上
  }
  ```
  Task 2（server 命令）与 Task 5（前端 `FeedStats` 类型）都消费这个形状，键名一字不许改。

- [ ] **Step 0: 提交 spec + 本计划（用户已确认随计划一起提）**

```bash
cd /Users/denny/Work/yibao
git add docs/superpowers/specs/2026-08-01-v1.1-consolidation-design.md \
        docs/superpowers/plans/2026-08-01-v1.1-slice0-measurement-base.md
git commit -m "docs(consolidation): v1.1 收口夯实迭代设计 + Slice 0 实施计划"
```

- [ ] **Step 1: 写失败测试**

在 `sidecar/tests/test_feed.py` 末尾追加：

```python
# ---------- 信任统计读模型（v1.1 Slice 0）----------
def _insert(feed, ts, kind, text="x", read=0, status="none"):
    """直接 SQL 插入（可指定 ts/read/status，feed.add 只写当前时刻）。"""
    feed._conn.execute(
        "INSERT INTO feed (ts, kind, text, meta, read, status) VALUES (?,?,?,'{}',?,?)",
        (ts, kind, text, read, status),
    )
    feed._conn.commit()


def test_stats_empty_db(tmp_path):
    from yibao_brain.feed import FeedStore

    feed = FeedStore(str(tmp_path / "f.db"))
    s = feed.stats()
    assert s["total"] == 0
    assert s["by_kind"] == {"task": 0, "reminder": 0, "event": 0}
    assert s["by_day"] == []
    assert s["read_rate"] == 0.0 and s["ignored_rate"] == 0.0
    feed.close()


def test_stats_by_kind_by_day_and_rates(tmp_path):
    import time

    from yibao_brain.feed import FeedStore

    feed = FeedStore(str(tmp_path / "f.db"))
    now = time.time()
    _insert(feed, now, "task", read=1)
    _insert(feed, now, "task")                              # 未读
    _insert(feed, now - 86400, "reminder", status="ignore")  # 恰好前一天（必跨日历日）
    s = feed.stats(days=7)
    assert s["total"] == 3
    assert s["by_kind"] == {"task": 2, "reminder": 1, "event": 0}
    assert s["read_rate"] == round(1 / 3, 4)
    assert s["ignored_rate"] == round(1 / 3, 4)
    assert len({row["day"] for row in s["by_day"]}) == 2    # 跨两天分组
    feed.close()


def test_stats_days_window_excludes_old(tmp_path):
    import time

    from yibao_brain.feed import FeedStore

    feed = FeedStore(str(tmp_path / "f.db"))
    now = time.time()
    _insert(feed, now - 8 * 86400, "task")   # 窗口外
    _insert(feed, now, "event")
    s = feed.stats(days=7)
    assert s["total"] == 1
    assert s["by_kind"]["task"] == 0 and s["by_kind"]["event"] == 1
    feed.close()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && uv run --extra dev pytest tests/test_feed.py -q -k stats`
Expected: FAIL，`AttributeError: 'FeedStore' object has no attribute 'stats'`

- [ ] **Step 3: 实现 stats()**

在 `sidecar/src/yibao_brain/feed.py` 的 `count_ignored` 方法之后插入：

```python
    def stats(self, days: int = 7) -> dict:
        """信任统计读模型（v1.1）：近 days 天主动行为聚合——按 kind/天计数 + 已读率/忽略率。
        读失败只 print 并返回全零结构（Feed 是增强面）。"""
        days = max(1, int(days))
        since = time.time() - days * 86400
        out = {
            "days": days, "since": since, "total": 0,
            "by_kind": {k: 0 for k in _KINDS},
            "by_day": [],
            "read_rate": 0.0, "ignored_rate": 0.0,
        }
        try:
            with self._lock:
                for row in self._conn.execute(
                    "SELECT kind, COUNT(*) AS n FROM feed WHERE ts >= ? GROUP BY kind", (since,)
                ):
                    out["by_kind"][row["kind"]] = int(row["n"])
                    out["total"] += int(row["n"])
                out["by_day"] = [
                    {"day": r["d"], "kind": r["kind"], "count": int(r["n"])}
                    for r in self._conn.execute(
                        "SELECT date(ts, 'unixepoch', 'localtime') AS d, kind, COUNT(*) AS n "
                        "FROM feed WHERE ts >= ? GROUP BY d, kind ORDER BY d DESC, kind",
                        (since,),
                    )
                ]
                row = self._conn.execute(
                    "SELECT COUNT(*) AS n, COALESCE(SUM(read), 0) AS r, "
                    "COALESCE(SUM(CASE WHEN status = 'ignore' THEN 1 ELSE 0 END), 0) AS i "
                    "FROM feed WHERE ts >= ?", (since,),
                ).fetchone()
                n = int(row["n"])
                if n:
                    out["read_rate"] = round(float(row["r"]) / n, 4)
                    out["ignored_rate"] = round(float(row["i"]) / n, 4)
        except Exception as e:
            print(f"[yibao] feed 统计失败（已降级为零）：{e}", file=sys.stderr)
        return out
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd sidecar && uv run --extra dev pytest tests/test_feed.py -q`
Expected: PASS（全文件，含既有用例）

- [ ] **Step 5: Commit**

```bash
git add sidecar/src/yibao_brain/feed.py sidecar/tests/test_feed.py
git commit -m "feat(feed): 信任统计读模型 stats()——按 kind/天聚合 + 已读率/忽略率"
```

---

### Task 2: server `feed_stats` IPC 命令

**Files:**
- Modify: `sidecar/src/yibao_brain/server.py`（`elif rtype == "feed":` 分支之后，约 1122 行后）
- Test: `sidecar/tests/test_server.py`

**Interfaces:**
- Consumes: Task 1 的 `FeedStore.stats(days)`。
- Produces: IPC 契约——壳发 `{"type": "feed_stats", "days": 7}`，脑回 `{"type": "feed_stats", "stats": <Task 1 的 dict>}`。Task 5 前端消费。

- [ ] **Step 1: 写失败测试**

在 `sidecar/tests/test_server.py` 的 `test_serve_async_feed_mark_read_lowers_unread` 之后追加（复用文件内已有 `_seed_feed` / `make_reader` / `_run_async` / `FakeProvider`）：

```python
def test_serve_async_feed_stats_roundtrip(tmp_path):
    """设置页信任统计：feed_stats 命令回近 N 天聚合（_seed_feed 预写两条）。"""
    _seed_feed(tmp_path / "a.db", [
        ("task", "任务A完成", {"task": {"id": "a"}}),
        ("event", "事件B", {}),
    ])
    out = []
    _run_async(
        serve_async(
            make_reader([{"type": "feed_stats"}]),
            lambda m: out.append(m),
            use_real=False,
            db_path=str(tmp_path / "a.db"),
            provider=FakeProvider(),
        )
    )
    msgs = [m for m in out if m["type"] == "feed_stats"]
    assert len(msgs) == 1
    stats = msgs[0]["stats"]
    assert stats["total"] == 2
    assert stats["by_kind"]["task"] == 1 and stats["by_kind"]["event"] == 1
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && uv run --extra dev pytest tests/test_server.py::test_serve_async_feed_stats_roundtrip -q`
Expected: FAIL（`msgs` 为空，断言 `len == 1` 不成立——server 还没这个分支）

- [ ] **Step 3: 实现命令分支**

在 `sidecar/src/yibao_brain/server.py` 的 `elif rtype == "feed":` 分支块（以 `})` 收尾，约 1122 行）之后插入：

```python
        elif rtype == "feed_stats":
            # 设置页「主动行为统计」：近 N 天主动行为聚合（默认 7 天）
            try:
                days = int(msg.get("days") or 7)
            except (TypeError, ValueError):
                days = 7
            write_msg({"type": "feed_stats", "stats": feed.stats(days=days)})
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd sidecar && uv run --extra dev pytest tests/test_server.py -q`
Expected: PASS（全文件）

- [ ] **Step 5: Commit**

```bash
git add sidecar/src/yibao_brain/server.py sidecar/tests/test_server.py
git commit -m "feat(feed): feed_stats IPC 命令——设置页信任统计取数"
```

---

### Task 3: watch 主动行为验收测试（经 dispatcher 落真实 FeedStore）

单侧触发逻辑已有 `test_watch.py` 31 例（久坐/静默/同段不重复/白名单/节流/预算/前台校验），dispatcher gating 已有 `test_proactive.py`（fake feed）。本任务补**唯一未测的链路**：事件经 `ProactiveDispatcher` 落**真实 SQLite FeedStore**（验收契约「该触发 100% 触发且记账、quiet 档只记账不打扰」），含 `BackgroundJobManager` 完成报告的线程→loop 真实路径。

**Files:**
- Create: `sidecar/tests/test_watch_acceptance.py`

**Interfaces:**
- Consumes: `HealthNudge.tick`（watch.py）、`ProactiveDispatcher.dispatch/.emit`（proactive.py）、`BackgroundJobManager.start(..., emit=)`（background_jobs.py）、`FeedStore.recent()`（feed.py）。不改任何被测代码。
- Produces: 无（纯验收测试）。

- [ ] **Step 1: 写测试文件（直接即最终形态——验收测试先行，跑通即证明现有链路满足契约；若不满足则暴露为红）**

创建 `sidecar/tests/test_watch_acceptance.py`：

```python
"""watch 主动行为验收（v1.1 Slice 0）：事件经 ProactiveDispatcher 落真实 FeedStore。

与 test_watch.py（行为单测）/test_proactive.py（fake feed gating）分工：
这里验「触发 → dispatcher → 真实 SQLite 记账」端到端契约，feed 用真库。
"""
import asyncio

from yibao_brain.background_jobs import BackgroundJobManager
from yibao_brain.feed import FeedStore
from yibao_brain.proactive import ProactiveDispatcher
from yibao_brain.watch import HealthNudge, WatchCtx, WatchSnapshot


def test_health_nudge_event_lands_in_real_feed(tmp_path):
    """久坐事件：full 档 → feed 记账（无 task meta → kind=event）+ write_msg 亮 pet 面。"""

    async def run():
        feed = FeedStore(str(tmp_path / "feed.db"))
        messages = []
        dispatcher = ProactiveDispatcher(
            settings={"proactive.level": "full", "proactive_voice": False},
            feed=feed, write_msg=messages.append,
        )
        nudge = HealthNudge(idle_warn_minutes=45, quiet_hours="")
        snap = WatchSnapshot(
            now=1000, activity={"state": "active", "seconds": 46 * 60, "segment_id": 1}
        )
        event = nudge.tick(snap, WatchCtx())
        assert event and event["kind"] == "reminder"
        await dispatcher.dispatch(event)
        items = feed.recent()
        feed.close()
        assert items and items[0]["text"] == "坐久了，起来活动一下吧 🧘"
        assert items[0]["kind"] == "event"
        assert messages and messages[0]["event"]["level"] == "full"

    asyncio.run(run())


def test_watch_command_completion_reports_into_feed_via_thread_emit(tmp_path):
    """后台命令完成：manager 工作线程 → dispatcher.emit（call_soon_threadsafe）
    → dispatch → feed 记账。quiet 档只记账不打扰（write_msg 为空）。"""

    async def run():
        feed = FeedStore(str(tmp_path / "feed.db"))
        messages = []
        dispatcher = ProactiveDispatcher(
            settings={"proactive.level": "quiet"},
            feed=feed, write_msg=messages.append,
            loop=asyncio.get_running_loop(),
        )
        manager = BackgroundJobManager()
        try:
            job = manager.start("exit 0", cwd=str(tmp_path), name="验收", emit=dispatcher.emit)
            for _ in range(100):
                if manager.status(job["task_id"])["status"] != "running":
                    break
                await asyncio.sleep(0.05)
            await asyncio.sleep(0.2)  # 让线程安全提交的 dispatch 协程跑完
        finally:
            manager.shutdown()
        items = feed.recent()
        feed.close()
        assert any("「验收」完成" in item["text"] for item in items), items
        assert messages == []  # quiet 档：记账但不打扰

    asyncio.run(run())
```

- [ ] **Step 2: 跑测试**

Run: `cd sidecar && uv run --extra dev pytest tests/test_watch_acceptance.py -q`
Expected: PASS。若 FAIL 即现有链路不满足验收契约——停下来报告，不在本任务修产品代码（修复属 Slice 2）。

- [ ] **Step 3: 全量回归**

Run: `cd sidecar && uv run --extra dev pytest -q`
Expected: 678+N 全 PASS

- [ ] **Step 4: Commit**

```bash
git add sidecar/tests/test_watch_acceptance.py
git commit -m "test(watch): 主动行为验收——dispatcher 落真实 feed + 后台命令线程 emit 链路"
```

---

### Task 4: TTS 三引擎验收脚本 `scripts/eval_tts.py`

判定逻辑（measure/evaluate）为纯函数进 pytest；真机合成手动跑（edge 需网络、云需 dashscope key、本地需模型目录，未配置的 provider 显式 SKIP）。

**Files:**
- Create: `sidecar/scripts/eval_tts.py`
- Create: `sidecar/tests/test_eval_tts.py`
- Modify: `sidecar/.gitignore`（加报告目录）

**Interfaces:**
- Consumes: `yibao_brain.voice` 的 `EdgeTtsSpeaker(voice)` / `CosyVoiceCloudSpeaker()` / `CosyVoiceSpeaker()`（构造即读 config/env），`speaker.available() -> bool`，`await speaker._synth_pcm(text) -> float32 ndarray | None`，`speaker.name`。
- Produces: `measure_provider(speaker, sentences) -> {"provider": str, "results": [{"text","ok","latency_s"}]}`（async）；`evaluate(measured, *, mem_bytes=None) -> {"provider","pass","failures","results","mem_bytes","latency_limit"}`；阈值常量 `LATENCY_LIMITS = {"edge": 1.0, "cosyvoice_cloud": 1.5, "cosyvoice": 3.0}`、`MEM_LIMIT_BYTES = 2 * 1024**3`。

- [ ] **Step 1: 写失败测试**

创建 `sidecar/tests/test_eval_tts.py`：

```python
"""eval_tts 判定逻辑单测（真机合成不进 pytest，只测 measure/evaluate 纯逻辑）。"""
import asyncio
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "scripts"))

import eval_tts  # noqa: E402


class _FakeSpeaker:
    def __init__(self, name, latencies):
        self.name = name
        self._latencies = list(latencies)

    async def _synth_pcm(self, text):
        await asyncio.sleep(self._latencies.pop(0))
        return b"\x00\x01" * 100


class _FailSpeaker:
    name = "edge"

    async def _synth_pcm(self, text):
        return None


def test_measure_provider_records_latency_and_ok():
    speaker = _FakeSpeaker("edge", [0.01, 0.01, 0.01])
    measured = asyncio.run(eval_tts.measure_provider(speaker, ["a", "b", "c"]))
    assert measured["provider"] == "edge"
    assert all(r["ok"] for r in measured["results"])
    assert all(r["latency_s"] >= 0.01 for r in measured["results"])


def test_measure_provider_marks_synth_failure():
    measured = asyncio.run(eval_tts.measure_provider(_FailSpeaker(), ["a"]))
    assert measured["results"][0]["ok"] is False


def test_evaluate_pass_within_limits():
    measured = {"provider": "edge", "results": [
        {"text": t, "ok": True, "latency_s": 0.5} for t in ("a", "b", "c")]}
    verdict = eval_tts.evaluate(measured)
    assert verdict["pass"] is True and verdict["failures"] == []


def test_evaluate_fails_on_synth_failure_slow_and_mem():
    measured = {"provider": "cosyvoice", "results": [
        {"text": "a", "ok": False, "latency_s": 0.1},
        {"text": "b", "ok": True, "latency_s": 9.9},
        {"text": "c", "ok": True, "latency_s": 0.1},
    ]}
    verdict = eval_tts.evaluate(measured, mem_bytes=3 * 1024**3)
    assert verdict["pass"] is False
    assert len(verdict["failures"]) == 3  # 合成失败 + 延迟超标 + 内存超标
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd sidecar && uv run --extra dev pytest tests/test_eval_tts.py -q`
Expected: FAIL，`ModuleNotFoundError: No module named 'eval_tts'`

- [ ] **Step 3: 实现 eval_tts.py**

创建 `sidecar/scripts/eval_tts.py`：

```python
"""TTS 三引擎验收（手动跑，非 CI）：固定 3 句 × provider，测成功率/起音延迟/（本地）峰值内存。

用法：
  uv run python scripts/eval_tts.py                 # 跑全部已配置 provider
  uv run python scripts/eval_tts.py --only edge

输出：终端逐行 PASS/FAIL + scripts/eval_reports/tts-<时间戳>.json
达标线（docs/superpowers/specs/2026-08-01-v1.1-consolidation-design.md §3）：
  成功率 100%；起音延迟 edge<1s / 云<1.5s / 本地<3s；本地峰值内存 <2GB。
  延迟在网络正常情况下计量；edge/云失败先查 key 与网络，异常在报告 failures 里注明。
"""
from __future__ import annotations

import argparse
import asyncio
import json
import sys
import time
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "src"))

SENTENCES = [
    "译宝你好，现在开始语音验收。",
    "北京时间十二点整，提醒你看一眼任务中心。",
    "这句话稍微长一点，用来观察流式合成的起音延迟和稳定程度。",
]

LATENCY_LIMITS = {"edge": 1.0, "cosyvoice_cloud": 1.5, "cosyvoice": 3.0}  # 起音延迟上限（秒）
MEM_LIMIT_BYTES = 2 * 1024**3  # 本地引擎峰值内存上限（2GB，arm64 spike 风险项）


def _peak_rss_bytes() -> int:
    """macOS 的 ru_maxrss 单位为字节（本脚本只在 macOS 上验收）。"""
    import resource

    return int(resource.getrusage(resource.RUSAGE_SELF).ru_maxrss)


async def measure_provider(speaker, sentences) -> dict:
    """逐句 _synth_pcm：ok=返回非空 PCM；latency_s=单句合成耗时（≈起音延迟）。"""
    results = []
    for text in sentences:
        t0 = time.perf_counter()
        pcm = await speaker._synth_pcm(text)
        latency = time.perf_counter() - t0
        results.append({
            "text": text,
            "ok": pcm is not None and len(pcm) > 0,
            "latency_s": round(latency, 3),
        })
    return {"provider": speaker.name, "results": results}


def evaluate(measured: dict, *, mem_bytes: int | None = None) -> dict:
    """对照达标线判定单 provider：全部成功 + 每句延迟 ≤ 上限 +（本地）内存 ≤ 2GB。"""
    provider = measured["provider"]
    limit = LATENCY_LIMITS[provider]
    failures = []
    for r in measured["results"]:
        if not r["ok"]:
            failures.append(f"合成失败：{r['text'][:12]}…")
        elif r["latency_s"] > limit:
            failures.append(f"延迟超标 {r['latency_s']}s > {limit}s：{r['text'][:12]}…")
    if mem_bytes is not None and mem_bytes > MEM_LIMIT_BYTES:
        failures.append(f"峰值内存超标 {mem_bytes / 1024**3:.2f}GB > 2GB")
    return {
        "provider": provider, "pass": not failures, "failures": failures,
        "results": measured["results"], "mem_bytes": mem_bytes, "latency_limit": limit,
    }


def _build_speakers() -> list:
    from yibao_brain.config import tts_voice
    from yibao_brain.voice import CosyVoiceCloudSpeaker, CosyVoiceSpeaker, EdgeTtsSpeaker

    return [EdgeTtsSpeaker(tts_voice()), CosyVoiceCloudSpeaker(), CosyVoiceSpeaker()]


async def _main() -> int:
    parser = argparse.ArgumentParser()
    parser.add_argument("--only", choices=sorted(LATENCY_LIMITS), default=None)
    args = parser.parse_args()

    reports = []
    for speaker in _build_speakers():
        if args.only and speaker.name != args.only:
            continue
        if not speaker.available():
            print(f"SKIP {speaker.name}：未配置或依赖缺失（见 .env.example 的 YIBAO_TTS_* / YIBAO_COSYVOICE_*）")
            continue
        before = _peak_rss_bytes()
        measured = await measure_provider(speaker, SENTENCES)
        mem = (_peak_rss_bytes() - before) if speaker.name == "cosyvoice" else None
        verdict = evaluate(measured, mem_bytes=mem)
        reports.append(verdict)
        mark = "PASS" if verdict["pass"] else "FAIL"
        lat = " / ".join(f"{r['latency_s']}s" for r in verdict["results"])
        mem_text = f" mem+{mem / 1024**3:.2f}GB" if mem is not None else ""
        print(f"{mark} {speaker.name}: 延迟 {lat}{mem_text}")
        for f in verdict["failures"]:
            print(f"  - {f}")

    out_dir = Path(__file__).resolve().parent / "eval_reports"
    out_dir.mkdir(exist_ok=True)
    out_file = out_dir / f"tts-{time.strftime('%Y%m%d-%H%M%S')}.json"
    out_file.write_text(json.dumps(reports, ensure_ascii=False, indent=2))
    print(f"报告已写 {out_file}")
    return 0 if reports and all(r["pass"] for r in reports) else 1


if __name__ == "__main__":
    sys.exit(asyncio.run(_main()))
```

- [ ] **Step 4: 报告目录加 .gitignore**

在 `sidecar/.gitignore` 末尾加一行：

```
scripts/eval_reports/
```

- [ ] **Step 5: 跑测试确认通过 + 全量回归**

Run: `cd sidecar && uv run --extra dev pytest tests/test_eval_tts.py -q && uv run --extra dev pytest -q`
Expected: PASS

- [ ] **Step 6: 真机冒烟（edge，有网即可；云/本地看配置）**

Run: `cd sidecar && uv run python scripts/eval_tts.py --only edge`
Expected: 打印 `PASS edge: 延迟 …`（或 FAIL + failures 明细），报告写入 `scripts/eval_reports/`。真机全量结果属于 Slice 1 基线报告，本任务只验证脚本能跑。

- [ ] **Step 7: Commit**

```bash
git add sidecar/scripts/eval_tts.py sidecar/tests/test_eval_tts.py sidecar/.gitignore
git commit -m "feat(eval): TTS 三引擎验收脚本——成功率/起音延迟/本地峰值内存对照达标线"
```

---

### Task 5: 设置页「主动行为统计」块端到端（Rust + brain.ts + SettingsView）

**Files:**
- Modify: `app/src-tauri/src/lib.rs`（事件转发约 449 行后、`get_feed` 命令约 772 行后、invoke_handler 约 1500 行）
- Modify: `app/src/lib/brain.ts`（`getFeedOnce` 之后，约 290 行后）
- Modify: `app/src/components/SettingsView.vue`（import 块 9-32 行、refs 区约 162 行后、onMounted 约 522 行、「主动协助」模板 704 行 `</template>` 前）

**Interfaces:**
- Consumes: Task 2 的 IPC `{"type":"feed_stats","stats":{days,since,total,by_kind,by_day,read_rate,ignored_rate}}`。
- Produces: `FeedStats` TS 类型 + `getFeedStatsOnce(days = 7, timeoutMs = 3000): Promise<FeedStats>`；Tauri 命令 `get_feed_stats(days: Option<u32>)`；事件 `brain-feed-stats`。

- [ ] **Step 1: Rust 事件转发**

在 `app/src-tauri/src/lib.rs` 的 `Some("feed") => { let _ = app.emit("brain-feed", v); }` 分支之后插入：

```rust
                            // 设置页信任统计响应：整体转发
                            Some("feed_stats") => {
                                let _ = app.emit("brain-feed-stats", v);
                            }
```

- [ ] **Step 2: Rust 命令**

在 `get_feed` 函数（约 772 行）之后插入：

```rust
/// 设置页信任统计：大脑回 {"type":"feed_stats","stats":…}，经 brain-feed-stats 事件广播。
#[tauri::command]
fn get_feed_stats(state: tauri::State<Brain>, days: Option<u32>) -> Result<(), String> {
    write_to_brain(
        &state,
        serde_json::json!({ "id": 0, "type": "feed_stats", "days": days.unwrap_or(7) }),
    )
}
```

并在 invoke_handler 注册列表里 `get_feed,`（约 1500 行）之后加一行 `get_feed_stats,`。

- [ ] **Step 3: cargo check**

Run: `cd app/src-tauri && cargo check`
Expected: 编译通过无 error

- [ ] **Step 4: brain.ts 取数函数**

在 `app/src/lib/brain.ts` 的 `getFeedOnce` 函数之后插入：

```ts
/** 信任统计（v1.1 收口）：近 N 天主动行为聚合，对应 sidecar FeedStore.stats()。 */
export interface FeedStats {
  days: number;
  since: number;
  total: number;
  by_kind: Record<string, number>;
  by_day: Array<{ day: string; kind: string; count: number }>;
  read_rate: number;
  ignored_rate: number;
}

const EMPTY_STATS: FeedStats = {
  days: 7, since: 0, total: 0, by_kind: {}, by_day: [], read_rate: 0, ignored_rate: 0,
};

/** 一次性取信任统计：发查询并等下一条 brain-feed-stats；大脑不在线/超时返回零值。 */
export async function getFeedStatsOnce(days = 7, timeoutMs = 3000): Promise<FeedStats> {
  const resp = new Promise<FeedStats>((resolve) => {
    void once<{ stats: FeedStats }>("brain-feed-stats", (ev) => resolve(ev.payload.stats));
  });
  const timeout = new Promise<FeedStats>((resolve) =>
    setTimeout(() => resolve(EMPTY_STATS), timeoutMs),
  );
  try {
    await invoke("get_feed_stats", { days });
  } catch { /* 大脑不在线：走超时兜底 */ }
  return Promise.race([resp, timeout]);
}
```

- [ ] **Step 5: SettingsView 统计块**

三处改动：

1. import：在 `app/src/components/SettingsView.vue` 第 9-32 行 `import { … } from "../lib/brain"` 列表中加入 `getFeedStatsOnce`（函数按字母/现有顺序插入即可），并在 import 块后加类型导入：

```ts
import type { FeedStats } from "../lib/brain";
```

2. refs + computed：在 `const autonErr = ref("");`（162 行）之后加：

```ts
// 主动行为统计（v1.1 信任仪表读模型）：只读展示，大脑不在线显示零值
const trustStats = ref<FeedStats | null>(null);
const trustSummary = computed(() => {
  const s = trustStats.value;
  if (!s) return "统计加载中…";
  return `近 ${s.days} 天共 ${s.total} 条 · 已读率 ${Math.round(s.read_rate * 100)}% · 忽略率 ${Math.round(s.ignored_rate * 100)}%`;
});
```

3. onMounted：在 `void getSettingsOnce().then(...)` 块（513-522 行）之后加：

```ts
  void getFeedStatsOnce().then((s) => { trustStats.value = s; });
```

4. 模板：在「主动协助」分区「通知方式」`</section>`（703 行）之后、`</template>`（704 行）之前插入：

```html
        <!-- 主动行为统计（v1.1 信任仪表读模型）：只读，数字全部来自 feed 表聚合 -->
        <section class="s-group">
          <div class="s-group-title">主动行为统计</div>
          <div class="s-note">{{ trustSummary }}</div>
          <div class="s-row">
            <span class="s-row-label">任务收尾播报</span>
            <span class="s-row-value">{{ trustStats?.by_kind.task ?? 0 }}</span>
          </div>
          <div class="s-row">
            <span class="s-row-label">提醒触发</span>
            <span class="s-row-value">{{ trustStats?.by_kind.reminder ?? 0 }}</span>
          </div>
          <div class="s-row">
            <span class="s-row-label">其它主动事件</span>
            <span class="s-row-value">{{ trustStats?.by_kind.event ?? 0 }}</span>
          </div>
        </section>
```

- [ ] **Step 6: 类型检查 + 构建**

Run: `cd app && npm run build`
Expected: vue-tsc 无 error，vite build 成功

- [ ] **Step 7: 人工目验**

`npm run tauri dev` → 设置 → 主动协助 → 底部「主动行为统计」卡：显示「近 7 天共 N 条 · 已读率 X% · 忽略率 Y%」+ 三行分类计数；大脑不在线时显示零值不报错。

- [ ] **Step 8: Commit**

```bash
git add app/src-tauri/src/lib.rs app/src/lib/brain.ts app/src/components/SettingsView.vue
git commit -m "feat(settings): 主动行为统计卡——feed_stats 端到端（Rust 转发 + brain.ts + 设置页）"
```

---

### Task 6: 点击评测场景采集（手动，非代码任务）

采集 ~12 个场景填满 `sidecar/scripts/eval_scenarios/`，为 Slice 1 点击基线备料。场景格式与采集器用法见该目录 README。**本任务只采集，跑评测出基线属 Slice 1。**

**Files:**
- Create: `sidecar/scripts/eval_scenarios/<name>.json` + `<name>.png` × 12（采集器生成，gt 手填）
- Delete: 仓库根空目录 `scripts/eval_scenarios/`（untracked 残留，`rmdir` 即可，无需提交）

- [ ] **Step 1: 清掉根目录空壳**

```bash
rmdir /Users/denny/Work/yibao/scripts/eval_scenarios
ls /Users/denny/Work/yibao/scripts/   # 确认为空目录已移除
```

- [ ] **Step 2: 采集 a11y 易档（计算器 + 系统设置，5 个）**

逐个把目标 app 放前台，再跑采集（在 `sidecar/` 下）：

```bash
# 计算器（先打开「计算器」app）
uv run python scripts/eval_click.py --capture --name calc_eq --target "等号按钮"
uv run python scripts/eval_click.py --capture --name calc_plus --target "加号按钮"
uv run python scripts/eval_click.py --capture --name calc_clear --target "清除按钮 C"
# 系统设置（打开「系统设置」）
uv run python scripts/eval_click.py --capture --name sysset_wifi --target "Wi-Fi 设置项"
uv run python scripts/eval_click.py --capture --name sysset_general --target "通用设置项"
```

- [ ] **Step 3: 采集 a11y 中档（Safari，2 个）**

```bash
# Safari 打开 apple.com.cn 放前台
uv run python scripts/eval_click.py --capture --name safari_nav_link --target "导航栏 Mac 链接"
uv run python scripts/eval_click.py --capture --name safari_tab_close --target "当前标签页关闭按钮"
```

- [ ] **Step 4: 采集 a11y 盲档（Canvas + Electron，5 个）**

```bash
# Canvas：Safari 打开 excalidraw.com 画一个矩形后
uv run python scripts/eval_click.py --capture --name canvas_excalidraw_rect --target "画布上的矩形"
# Canvas：任意 canvas 小游戏页面
uv run python scripts/eval_click.py --capture --name canvas_game_el --target "画布中的开始按钮"
# Electron：VS Code 放前台
uv run python scripts/eval_click.py --capture --name vscode_explorer --target "资源管理器图标"
uv run python scripts/eval_click.py --capture --name vscode_tab --target "当前编辑器标签页"
uv run python scripts/eval_click.py --capture --name vscode_statusbar --target "状态栏分支名"
```

- [ ] **Step 5: 手填 GT**

逐个编辑生成的 `<name>.json`：把 `gt` 填成 `{"kind":"region","rect":[x1,y1,x2,y2]}`（逻辑坐标，目标控件的外接框；点状目标用 `{"kind":"point","xy":[x,y]}`）。窗口裁剪场景按 README 补 `"scale"`。**参照截图目测取框，框要贴控件边缘不留余量**——SoM 命中率判定按框内命中。

- [ ] **Step 6: 验收采集完整性**

```bash
ls sidecar/scripts/eval_scenarios/*.json | wc -l   # 期望 12
ls sidecar/scripts/eval_scenarios/*.png | wc -l    # 期望 12
python3 -c "
import json, glob
for f in sorted(glob.glob('sidecar/scripts/eval_scenarios/*.json')):
    sc = json.load(open(f))
    gt = sc.get('gt') or {}
    ok = ('rect' in gt) or ('xy' in gt)
    print(('OK ' if ok else 'MISSING_GT '), f)
"
```
Expected: 12 个 OK。场景文件是否入库由采集者定（截图含屏幕内容，建议不入库、把 `sidecar/scripts/eval_scenarios/*.png` 留本地；json 若入库注意剔除隐私窗口标题）。本任务不做 git 操作。

---

## Self-Review 记录

- **Spec 覆盖**：spec §4 四项 → Task 6（场景采集+空壳处理）、Task 3（watch 验收 pytest）、Task 4（TTS 脚本）、Task 1/2/5（统计读模型+feed_stats+设置页块）。无遗漏。
- **类型一致性**：`stats` dict 键（days/since/total/by_kind/by_day/read_rate/ignored_rate）在 feed.py ↔ server.py ↔ Rust 透传 ↔ brain.ts `FeedStats` ↔ SettingsView 模板五处一致；`getFeedStatsOnce(days=7)` ↔ `get_feed_stats(days: Option<u32>)` ↔ `msg.get("days")` 一致。
- **占位符扫描**：无 TBD/TODO；每个代码步骤含完整代码与验证命令。
